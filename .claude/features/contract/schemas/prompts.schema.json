{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "prompts.schema.json",
  "schema_version": "1.0.0",
  "owner": "rabbit-workflow team",
  "deprecation_criterion": "when prompt-contract assembly becomes native to Claude Code",
  "title": "Feature PROMPTS",
  "description": "Declarations of prompt-contract entries owned by a feature. Each entry names a prompt id, the kind of consumer (skill or subagent), the inject list of policy/context paths assembled at invocation, and the slots filled by the template at .claude/features/contract/templates/prompts/<id>.txt.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["id", "kind", "inject", "slots"],
    "additionalProperties": false,
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9-]*$"
      },
      "kind": {
        "type": "string",
        "enum": ["skill", "subagent"]
      },
      "inject": {
        "type": "array",
        "minItems": 1,
        "items": {"type": "string"}
      },
      "slots": {
        "type": "array",
        "items": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_]*$"
        }
      }
    }
  }
}
