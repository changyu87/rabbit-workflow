# /rabbit-workspace

Print an annotated ASCII hierarchy of the rabbit workflow workspace.

## Usage

/rabbit-workspace
/rabbit-workspace --full

## Implementation

Delegates to `.claude/features/rabbit-cage/scripts/workspace-tree.sh`.
