# Contract — root-management

## Reads
- `artifacts/install.sh`, `artifacts/CLAUDE.md`, `artifacts/README.md`

## Writes
- Same three files (via breeder dispatch with active scope only).

## Invokes
- Nothing at runtime.
