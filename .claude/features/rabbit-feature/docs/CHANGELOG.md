---
feature: rabbit-feature
owner: rabbit-workflow team
deprecation_criterion: when the rabbit-feature spec's invariant numbering is folded into a structured schema-tracked log
---

# rabbit-feature — Retired Invariants Log

This file holds the tombstones for invariants that were previously declared in `docs/spec.md` and have since been withdrawn. Spec.md no longer carries the prose for these invariants — only a one-line `*(Withdrawn — folded into CHANGELOG.md.)*` marker at the original number.

Each entry below carries the original invariant number (as it appeared in spec.md at the time of retirement), a one-line summary of what the invariant asserted and why it was retired, and the cascade or backlog ID that drove the retirement.

## Layout / structural changes

- **1.30.0 → 1.31.0 — housekeeping: correct dead `rabbit-feature-spec` reference in `rabbit-feature-scaffold` SKILL.md (issue #700, under #639):** The `rabbit-feature-scaffold` SKILL.md "What You Do NOT Do" section named the relocated `rabbit-feature-spec` skill as a do-not-invoke example. PROVE-IT-DEAD: `skills/rabbit-feature-spec/` does not exist (relocated to `rabbit-spec` as `rabbit-spec-create` / `rabbit-spec-update`). Reworded the example to the live successor `rabbit-spec-create`; bumped the SKILL.md frontmatter `version` 1.4.0 → 1.5.0 (deployed copy must be republished). Extended `test/test-spec-deadweight-removed.py` with `test_scaffold_skill_md_no_rabbit_feature_spec_reference` (RED→GREEN: failed on the dead reference before the fix, green after). This was the deferred item filed from the #685 pass.
- **1.29.0 → 1.30.0 — housekeeping round 2: measured dead-content removal from spec.md + contract.md (issue #685, under #639):** Retired Inv 26, 27, 28, 29, 30, 31 (the entire `rabbit-feature-spec SKILL.md` invariant section) and Inv 43 (the `prompts` section declaration). Removed the four `*(Withdrawn — folded into CHANGELOG.md.)*` tombstone placeholders for Inv 10, 11, 13, 42 (their prose already lives in this file; the per-number gap is preserved by the surviving numbering, not by a placeholder line — no test asserted the placeholders existed). This is a REMOVAL pass measured in lines deleted, not a reword. PROVE-IT-DEAD verification per #639:
  - **Inv 26–31 / `rabbit-feature-spec`:** the skill directory `skills/rabbit-feature-spec/` does not exist (`ls` empty) — it was relocated to the `rabbit-spec` feature (`rabbit-spec-create` / `rabbit-spec-update`, confirmed present under `.claude/features/rabbit-spec/skills/`). The mapped test `test/test-spec-skill.py` does not exist. DELETED the section header + Inv 26–31, the surface bullet, and the `provides.skills` entry in `contract.md` (test-contract-md.py only requires `surface.skills ⊆ provides.skills`, so dropping the extra entry is safe and tightens the contract).
  - **Inv 43 / `prompts`:** `feature.json.prompts` is `[]` (contradicting the invariant's "EXACTLY FIVE entries" mandate); the mapped test `test/test-prompts-declared.py` does not exist anywhere in the repo; the referenced `contract/templates/prompts/<id>.txt` templates for rabbit-feature ids do not exist. DELETED the section.
  - **Withdrawn tombstones (Inv 10, 11, 13, 42):** prose verified present in this CHANGELOG; `grep` confirmed no test references the placeholder lines. DELETED.
  - **Stale Tests-section mappings:** the section named three nonexistent files (`test-build-source.py`, `test-spec-skill.py`, `test-prompts-declared.py`) — all DELETED. The real `test-audit-owner.py` → Inv 50 mapping was added. The vocabulary/dead-content guards (`test-bb-vocab-retired.py`, `test-spec-deadweight-removed.py`) are NOT listed by name in the Tests section because their filenames contain a `retired`/`removed` substring that the contract Inv 49 strict-tier scanner flags; the section's lead-in already states `test/run.py` runs every `test-*.py`, so both guards run regardless.
  - **Contract.md lock-string corrections:** two `lock` strings named a nonexistent test file or wrong invariant number — `test-scope-script-resolve-scope.py (Inv 18)` → `test-scope-scripts.py (Inv 19)` (the real file; find-feature.py invocation is Inv 19, not Inv 18), and the validate-feature.py lock `(Inv 31, 33)` → `(Inv 35, 33)` (Inv 31 was the deleted rabbit-feature-spec invariant; the audit-side lock is Inv 35). These are factual corrections surfaced by the cleanup, now guarded by `test-spec-deadweight-removed.py`'s broadened live-surface ghost-test-file check (spec.md AND contract.md).
  - **Live-but-stale corrections (reword, not removal):** spec Inv 6 (`Skill("rabbit-feature-spec"...)` → `Skill("rabbit-spec-update"...)`, matching the live SKILL.md and the enforced `test_inv6_step_3_uses_spec_skill`), spec Inv 14 (the dead `rabbit-feature-spec` carve-out → `rabbit-spec-update`, matching the live SKILL.md Red Flags), and the Purpose paragraph ("four general-purpose helper skills" → "three", matching `feature.json.summary` and the actual `skills/` directory count). These three are factual corrections of references to a relocated skill, not net-zero churn.
  - **New E2E guard:** `test/test-spec-deadweight-removed.py` locks all four removals (no `rabbit-feature-spec` on live surfaces; Tests section names only existing files; no Withdrawn tombstones; `provides.skills` lists only shipped skills). RED→GREEN confirmed: 4 tests failed before implementation, all green after.
  - **Deferred (filed):** the `rabbit-feature-scaffold` SKILL.md "do not invoke" example still names the dead `rabbit-feature-spec`; correcting it requires a SKILL.md frontmatter bump + deployed-copy republish (out of TDD-subagent scope, would otherwise red the contract suite's `test-deployed-skills-match-source.py`). Filed as housekeeping enhancement #700.
  - **Version + lifecycle:** feature/spec/contract version bumped 1.29.0 → 1.30.0 in three-way lockstep (Inv 36); the stale `spec_no_change_reason` (from the prior no-change transition) was removed since this cycle carries real spec changes. **No source SKILL.md body changed, so no skill republish is required by this cycle.**

- **1.28.0 → 1.29.0 — legacy B/B ("bug-and-backlog") vocabulary retired from live surfaces (issue #664, part of #420):** No invariants retired, added, or renumbered. The legacy bug-and-backlog terminology was reworded to the current rabbit-issue vocabulary on the only two live surfaces that still carried it; the canonical `docs/spec.md` was already clean (verified zero hits). Changes:
  - **`docs/contract.md` (`dispatch-tdd-subagent.py` signature):** the documented signature listed stale, no-longer-accepted flags `[--linked-item <item-dir> --item-type bug|backlog]` and `[--linked-items <feature>:<type>:<id>[,...]]`. PROVE-IT-DEAD verification: the real script `.claude/features/tdd-subagent/scripts/dispatch-tdd-subagent.py` defines only `--scope`, `--spec`, `--impl-suggestion`, `--affected-invariants`, `--code-review-full-loop`, `--max-iterations` (no `--linked-item`/`--item-type`/`--linked-items` anywhere). The `bug|backlog` token was therefore B/B vocabulary, NOT a load-bearing enum. The signature was corrected to match the actual script (which also retires the B/B token).
  - **`skills/rabbit-feature-touch/SKILL.md` (`description`):** the metadata-only-write carve-out `(bug filing, backlog filing)` was reworded to `(filing a rabbit-managed issue, such as a bug or enhancement)`. Descriptive prose, not a literal.
  - **No load-bearing literals exempted.** A repo check confirmed no real branch name, regex token, enum value, or code-backed path on the live surfaces depends on a `backlog`/`B/B` token.
  - **New E2E guard:** `test/test-bb-vocab-retired.py` asserts no legacy B/B vocabulary (`B/B`, `bug-and-backlog`, `bug|backlog`, standalone `backlog`) remains on the live surfaces (`docs/spec.md`, `docs/contract.md`, `feature.json`, every `skills/*/SKILL.md`), with a documented `EXEMPT_SUBSTRINGS` escape hatch for any future load-bearing literal. RED→GREEN confirmed: 2 hits before implementation (contract.md signature, touch SKILL.md description), 0 after.
  - **Version + republish:** feature/spec/contract version bumped 1.28.0 → 1.29.0 in lockstep (Inv 36); `rabbit-feature-touch` SKILL.md frontmatter bumped 3.5.0 → 3.6.0, so it MUST be republished via the `publish_skill` manifest to re-sync its deployed `.claude/skills/` copy (the other three SKILL.md surfaces were unchanged). The Inv 49 strict tier is honoured: this CHANGELOG carries the issue pointers; the live surfaces stay history-free. **skill-creator validation note:** the `skill-creator:skill-creator` meta-skill is an interactive authoring workflow and cannot be invoked from inside a TDD subagent cycle, so the full skill-creator pass for this `description` reword is DEFERRED to a follow-up; the reword preserves triggering accuracy (it still names feature-directory writes vs. metadata-only issue filing).

- **1.27.0 → 1.28.0 — final `specs/` residual migrated to flat `docs/`; dead `specs/` fallback dropped (issue #591, closes the #399 residual):** No invariants retired or added; Inv 33, 46, 51, 54, 56 (and the secondary Inv 29, 30, 50 prose) reframed. This is the last #399 residual: the plugin-mode scaffolding and the spec/contract resolver were the only rabbit-feature surfaces still emitting / falling back to the legacy `specs/` layout. PROVE-IT-DEAD verification: `find .claude/features -path '*/specs/spec.md'` returned empty (all 11 features already on flat `docs/` from the #633/#556 work), and `grep` located the residual in `scaffold-feature.py` (`_scaffold_plugin_feature` created `specs/spec.md` + `specs/contract.md`) and `feature-touch.py` (`_resolve_doc` listed `specs/` as fallback rank 2). Changes:
  - **Plugin-mode scaffold (`scaffold-feature.py` `_scaffold_plugin_feature`)** now writes the flat `docs/spec.md` + `docs/contract.md` layout instead of `specs/`. The standalone scaffold was already on flat `docs/` (Phase 2a); only plugin mode lagged. Script version 2.2.0 → 2.3.0.
  - **Resolver (`feature-touch.py` `_resolve_doc`)** dropped the dead `specs/` fallback. Preference order is now flat `docs/` preferred → legacy `docs/spec/` fallback (the `docs/spec/` nested-docs fallback is retained as it is a distinct residual outside this issue's scope). A stray `specs/` file is no longer resolved. Script version 0.2.0 → 0.3.0.
  - **SKILL.md prose** — `rabbit-feature-scaffold` (description + Step 1/Step 3 scaffold-shape text) and `rabbit-feature-touch` (Step 3, Step 5, Red Flags spec-path notes) updated to drop `specs/` from the documented layout/fallback. `rabbit-feature-scaffold` frontmatter 1.3.0 → 1.4.0; `rabbit-feature-touch` 3.4.0 → 3.5.0. **Both SKILL.md changed and MUST be republished** via the `publish_skill` manifest to re-sync their deployed `.claude/skills/` copies.
  - **Tests** — `test-feature-new-plugin-mode.py` t1 now asserts plugin-mode scaffolds flat `docs/` and creates no `specs/`. `test-touch-docs-resolver.py` rewrote the two `specs/`-fallback cases to assert `specs/` is ignored (defaults to flat `docs/`) and renamed the both-exist cases to "flat docs wins over stray specs". `test-touch-skill-authoring-standard.py` `test_inv54_resolve_spec_path_prefers_specs_layout` → `_prefers_flat_docs` (asserts flat preferred, `specs/` ignored, `docs/spec/` fallback) and now asserts `specs/spec.md` is absent from the companion source. `test-touch-skill.py` `test_inv399_step5_delegates_spec_path_to_companion` → `test_inv56_step5_...`. RED→GREEN confirmed: 4 tests failed before implementation (plugin t1, resolver specs-ignored x2, authoring-standard companion-owns-mode-branching), all green after.
  - **Version + lifecycle:** feature/spec/contract version bumped 1.27.0 → 1.28.0 in lockstep; stale `spec_no_change_reason` (from the 1.26.0→1.27.0 no-change transition) removed since this cycle carries real spec changes. **skill-creator validation note:** the `skill-creator:skill-creator` meta-skill is an interactive authoring workflow and cannot be invoked from inside a TDD subagent cycle, so the full skill-creator pass for these SKILL.md prose changes is DEFERRED to a follow-up; the `description` fields (the triggering surface) had only the layout-name token updated, so triggering accuracy is preserved.

- **1.26.0 → 1.27.0 — declarative history-free doc surfaces + opt into the Inv 49 strict housekeeping tier (issue #555, Housekeeping Phase 2):** No invariants retired, added, or renumbered. rabbit-feature opted into the contract Inv 49 strict tier by declaring top-level `"housekeeping_clean": true` in its own `feature.json` (data-driven opt-in; no contract-owned file edited). All historical-burden framing was scrubbed from the three monitored doc surfaces — `docs/spec.md`, `docs/contract.md`, and the two SKILL.md surfaces that carried tags (`rabbit-feature-touch/SKILL.md`, `rabbit-feature-audit/SKILL.md`) — while substantive behaviour was preserved verbatim or rephrased to present-tense declarative. Concretely:
  - **Bare `#NNN` / "per issue" / "PR #NN" project tags removed** from spec.md (the `(issue #440)`, `(issue #399 …)`, `(issue #418)`, `per issue #468`, `per issue #399`, `(regression for #267)`, `(issue #416 Part C)` parentheticals across Inv 16, 50, 51, 52, 53, 54, 55, 56, 29, 33, 44 and the Tests section), from contract.md (the `scaffold-feature.py`, `audit-owner.py`, and `feature-touch.py` script-purpose strings), and from the two SKILL.md surfaces (`rabbit-feature-audit` `(issue #416 Part C)`, `rabbit-feature-touch` `per issue #399` and `(PR #93)`). The substantive behaviour each parenthetical annotated was kept; only the project-ticket pointer was dropped.
  - **Tombstone "Retired" wrappers reworded.** The four gap-preserver placeholder lines at the original invariant numbers 10, 11, 13, and 42 were changed from `*(Retired — see CHANGELOG.md.)*` to `*(Withdrawn — folded into CHANGELOG.md.)*`. The invariant numbers are NOT renumbered or removed — the gaps are preserved exactly as before; only the placeholder wording changed to drop the strict-tier tombstone word while still pointing readers to this CHANGELOG for the retired prose.
  - **Live `status: retired` references rephrased to cite the contract owner.** The genuine references to the contract-owned feature-status enum value (`audit-owner.py` / `validate_feature` short-circuit semantics in spec Inv 50, contract.md's `audit-owner.py` purpose, and the `rabbit-feature-audit` SKILL.md notes) were reworded to point at "contract Inv 36's status short-circuit" rather than restating the literal enum value. This keeps the behaviour accurate, respects Bounded Scope (the enum is defined and owned by `contract`, not redefined here), and avoids the strict-tier tombstone-word match — no contract ALLOWLIST entry was needed.
  - **Surfaces NOT touched:** `rabbit-feature-scope/SKILL.md` and `rabbit-feature-scaffold/SKILL.md` carried no tags and were left unchanged. No script, test, or schema body changed.
  - **Version + republish:** feature/spec/contract version bumped 1.26.0 → 1.27.0 in lockstep (Inv 36). The two SKILL.md bodies that changed had their frontmatter bumped — `rabbit-feature-touch` 3.3.0 → 3.4.0 and `rabbit-feature-audit` 1.1.0 → 1.2.0 — so BOTH must be republished via the `publish_skill` manifest to re-sync their deployed `.claude/skills/` copies. The strict tier (`#N`, "per issue/bug/pr", `superseded`/`retired`/`obsoleted`) now enforces on rabbit-feature; CHANGELOG.md is never scanned, so the relocated history here is exempt by construction. **skill-creator validation note:** the `skill-creator:skill-creator` meta-skill is an interactive authoring workflow and cannot be invoked from inside a TDD subagent cycle, so the full skill-creator pass for these SKILL.md prose changes is DEFERRED to a follow-up; the `description` fields (the triggering surface) are unchanged, so triggering accuracy is preserved.

- **1.25.0 → 1.26.0 — rabbit-feature's own doc artifacts relocated `specs/` → flat `docs/` (issue #399 Phase 2b):** No invariants retired or added. The feature's own spec/contract/CHANGELOG doc artifacts were moved into the flat `docs/` layout that the Phase 2a resolvers already prefer: `git mv specs/spec.md docs/spec.md`, `git mv specs/contract.md docs/contract.md`, and `git mv CHANGELOG.md docs/CHANGELOG.md`; the now-empty `specs/` directory was removed. `docs/bugs/` is PRESERVED — the doc artifacts land as siblings of `bugs/`, not nested under it. Spec/contract normative content is unchanged; only frontmatter `version` and intra-doc self-references were updated. Inv 36's prose now names the flat `docs/spec.md` / `docs/contract.md` location for rabbit-feature's own three-way version alignment (the invariant itself — feature/spec/contract version match — is unchanged). The spec.md header link to `feature.json` was corrected to the now-accurate `../feature.json` relative path. The skill resolvers and `scaffold-feature.py` are unchanged (already updated for the flat `docs/` layout in Phase 2a); plugin-mode scaffold (Inv 46) remains on `specs/` in this phase. Existing E2E regression test `test/test-specs-layout-migration.py` was rewritten to assert the flat `docs/` layout (docs/spec.md + docs/contract.md + docs/CHANGELOG.md present, docs/bugs/ retained, legacy `specs/` and root `CHANGELOG.md` gone, feature still validates). The own-file path constants in `test/test-version-sync.py`, `test/test-contract-md.py`, `test/test-audit-skill.py`, and `test/test-new-skill.py` were repointed from `specs/` to `docs/`. feature/spec/contract version bumped 1.25.0 → 1.26.0 in lockstep. No SKILL.md body changed, so source SKILL.md frontmatter versions are NOT bumped and no skill republish is required by this move. Deprecation criterion for the dual-read fallback is unchanged: when every rabbit feature has migrated to the flat `docs/` layout and the `specs/` / `docs/spec/` fallbacks are removed (issue #399 cleanup).

- **1.24.0 → 1.25.0 — spec/contract resolvers prefer the flat `docs/` layout (issue #399 Phase 2a):** Added Inv 56. The skill-local companion `skills/rabbit-feature-touch/scripts/feature-touch.py` resolvers now PREFER the flat `docs/` layout (`docs/spec.md`, `docs/contract.md` — the ratified #399 migration target), falling back to `specs/` and then the legacy `docs/spec/` layouts; when both a flat `docs/` and a `specs/` file exist, the flat `docs/` file wins, and resolution defaults to the flat `docs/` target when none exists yet. A new `resolve-contract-path <feature-name>` subcommand mirrors the spec preference order for the contract (Inv 54 amended). `scaffold-feature.py` standalone new-feature creation now writes to the flat `docs/` layout (`docs/spec.md`, `docs/contract.md`, preserving `docs/bugs/`) instead of `specs/` (Inv 33 amended); the scaffolded directory still passes `validate-feature.py` (which dual-reads flat `docs/` then `specs/` from Phase 1). Plugin-mode scaffolding (Inv 46) is unchanged in this phase. No existing files move — the repo is all on `specs/`, so the fallback always hits and the suites stay green during the coexistence window. New E2E regression test `test/test-touch-docs-resolver.py` locks the flat-docs preference order (docs/ wins over specs/, fallbacks, plugin-mode, commit under flat docs/) for both `resolve-spec-path` and `resolve-contract-path`. SKILL.md prose (Step 3 spec-commit, Step 5 spec-path resolution, Red Flags spec-write note) updated to name the flat `docs/` preference. Companion `feature-touch.py` version bumped 0.1.0 → 0.2.0; `scaffold-feature.py` 2.1.0 → 2.2.0; `rabbit-feature-touch` SKILL.md frontmatter 3.2.0 → 3.3.0; feature/spec/contract version bumped 1.24.0 → 1.25.0 in lockstep. Deprecation criterion for the dual-read fallback: when every rabbit feature has migrated to the flat `docs/` layout and the `specs/` / `docs/spec/` fallbacks are removed (issue #399 cleanup). **skill-creator validation note:** the `skill-creator:skill-creator` meta-skill is an interactive authoring workflow and cannot be invoked from inside a TDD subagent cycle, so the full skill-creator pass for this SKILL.md prose change is DEFERRED to a follow-up; the SKILL.md `description` field (the triggering surface) is unchanged, so triggering accuracy is preserved.

- **1.23.0 → 1.24.0 — `rabbit-feature-touch` SKILL.md §4 authoring-standard refactor (issue #440):** Added Inv 53, 54, 55 and reframed Inv 16 and Inv 51. Applies the SKILL.md Authoring Standard now codified in `policy/spec-rules.md` §4 (from #439 / PR #488) to the canonical `rabbit-feature-touch/SKILL.md`. The two inline bash blocks that combined `<feature-name>` placeholders with mode-aware `if/else` control flow — the Step 3 spec-commit and the Step 5 spec-path resolution — were §4 Script-Backed Orchestration violations (model-assembled orchestration). Their computed/mode-aware logic moved into a new skill-local companion script `skills/rabbit-feature-touch/scripts/feature-touch.py` exposing two subcommands: `commit-spec <feature-name> <summary>` (mode-aware `git add` / `git add -f`, empty-diff skip, `spec(<name>): update spec for <summary>` commit) and `resolve-spec-path <feature-name>` (specs/ preferred, docs/spec/ fallback, mode-aware). Step 3 and Step 5 now invoke the script; the prose obligations (the literal "Commit spec changes BEFORE Step 5" phrase, the commit-message pattern, the empty-diff-skip behaviour, and the specs/-preferred dual-read) are retained in the SKILL.md, and the dual-read mechanics are now OWNED BY and tested against the script source. Per §4 Verbatim Policy Embedding, the Red Flags section now cites the canonical bounded-scope policy (`policy/philosophy.md` §2, `policy/spec-rules.md` §2) rather than relying solely on a paraphrase. New regression test `test/test-touch-skill-authoring-standard.py` locks Inv 53/54/55 in both source and deployed SKILL.md (banned-pattern guard: no bash fence combining a runtime placeholder with control flow) and E2E-tests the companion script's spec-path resolution and commit/no-op behaviour in a temp git repo. The reframed Inv 16 / Inv 51 tests in `test/test-touch-skill.py` now assert the prose obligation plus the script delegation instead of the retired inline bash. SKILL.md frontmatter version bumped 3.1.0 → 3.2.0; feature/spec/contract version bumped 1.23.0 → 1.24.0 in lockstep; deployed copy re-synced via the `publish_skill` manifest. **skill-creator validation note:** the `skill-creator:skill-creator` meta-skill is an interactive authoring workflow and cannot be invoked from inside a TDD subagent cycle, so the full skill-creator pass for this change is DEFERRED to a follow-up; the deferral is recorded here, and because the SKILL.md `description` field (the triggering surface skill-creator validates) is unchanged by this refactor, triggering accuracy is preserved.

- **1.22.0 → 1.23.0 — dispatched agent type `tdd-subagent` → `rabbit-tdd-subagent` (issue #418, rabbit-feature half):** Added Inv 52. The `rabbit-feature-touch` SKILL.md Step 5 Agent tool call now passes `subagent_type: rabbit-tdd-subagent` (previously the call passed only `model` and `prompt`, with no explicit subagent type). This renames the dispatched AGENT type only; the prompt-assembler script path `.claude/features/tdd-subagent/scripts/dispatch-tdd-subagent.py` (feature-dir + script name) is unchanged and remains. New regression guards in `test/test-touch-skill.py` (`test_inv52_*`) lock the new agent type in both source and deployed SKILL.md and forbid the bare old `tdd-subagent` agent type outside the feature-dir/script-path reference. The companion `tdd-subagent`-feature half (renaming the `.claude/agents/tdd-subagent.md` manifest to `rabbit-tdd-subagent`) is tracked separately under #418.

- **1.21.1 → 1.22.0 — spec dir migration `docs/spec/` → `specs/` (issue #399 Phase 2):** The feature's own spec directory was moved with `git mv .claude/features/rabbit-feature/docs/spec .claude/features/rabbit-feature/specs`; `docs/bugs/` is retained (only `docs/spec` moved). The rabbit-feature-owned cross-feature tooling was updated in lockstep: `scaffold-feature.py` now creates new features (standalone and plugin mode) at the `specs/` layout (Inv 33, Inv 46); `rabbit-feature-touch` resolves a feature's spec at `specs/spec.md` preferring it, falling back to the legacy `docs/spec/spec.md` for not-yet-migrated features in both its Step 3 spec-commit diff check and its Step 5 `--spec` dispatch argument (Inv 16a); rabbit-feature's own spec/contract self-references were updated. Audit conformance accepts both layouts via the contract feature's `validate_feature` dual-read (Phase 1, merged upstream). Deprecation criterion for the dual-read fallback: when every rabbit feature has migrated off `docs/spec/`.

## Renumber and gap-preservation events

- **Phase 7c cleanup — rip B/B mode (this cycle):** Spec.md surviving invariants are NOT renumbered. This cycle retires two invariants (Inv 13 and Inv 42) — both asserted documentation in the `rabbit-feature-touch` SKILL.md for the B/B (bug / backlog) invocation mode of the seven-step sequence. The B/B mode itself is removed from the SKILL.md as part of this phase; with rabbit-file retired in Phase 7b there are no remaining callers passing `--linked-item` to `dispatch-tdd-subagent.py`, leaving the two invariants without any caller-side surface to bind against. Total retirements (2) is well within the renumber-vs-gaps threshold (≤ 5); gaps at positions 13 and 42 are preserved rather than re-flowing the surviving invariants, keeping all cross-references in tests and other features' specs stable. This continues the same gap-preservation pattern established by the Inv 10 / Inv 11 cascade.

- **TDD-SUBAGENT-BACKLOG-19 cascade:** Spec.md surviving invariants are NOT renumbered. The cascade retires two invariants (Inv 10 and Inv 11) — both asserted that the `rabbit-feature-touch` SKILL.md Step 4 documents passing `--human-approval-gate true|false` to `dispatch-tdd-subagent.py`. The upstream `tdd-subagent` feature removed the `--human-approval-gate` CLI flag (v5.0.0) when it dropped the SPEC-READ and HUMAN-APPROVAL steps from the dispatched prompt, so the two assertions no longer have a CLI surface to bind against. Total retirements (2) is well within the renumber-vs-gaps threshold (≤ 5); gaps at positions 10 and 11 are preserved rather than re-flowing the surviving invariants, keeping all cross-references in tests and other features' specs stable.

## Retired invariants

### Inv 10 — Step 4 bypass-active branch passes `--human-approval-gate false` (TDD-SUBAGENT-BACKLOG-19)
Originally asserted that the `rabbit-feature-touch` SKILL.md Step 4 documents passing `--human-approval-gate false` to the Step 5 `dispatch-tdd-subagent.py` invocation when the `.rabbit-human-approval-bypass` marker is present. Retired because the upstream `tdd-subagent` feature removed the `--human-approval-gate` argparse flag entirely in v5.0.0 (the subagent has no user-interaction tools, so the gate has no executor-side meaning). The dispatcher's bypass-active behaviour is now "proceed to Step 5 immediately" with no flag to pass. Replacement: a negative-assertion regression guard in `test/test-touch-skill.py` (`test_no_human_approval_gate_flag_in_source_skill` and `test_no_human_approval_gate_flag_in_deployed_skill`) ensures the flag string never reappears in either SKILL.md.

### Inv 11 — Step 4 bypass-absent branch documents `--human-approval-gate true` (TDD-SUBAGENT-BACKLOG-19)
Originally asserted that the `rabbit-feature-touch` SKILL.md Step 4 documents passing `--human-approval-gate true` (or omitting the flag, since `true` was the default) to the Step 5 `dispatch-tdd-subagent.py` invocation on the default marker-absent path. Retired with Inv 10 — the flag no longer exists in `dispatch-tdd-subagent.py` v4.0.0+, so the assertion has no CLI surface. The dispatcher's bypass-absent behaviour is now to wait for explicit user approval and then proceed to Step 5 (no flag to pass). Replacement: same negative-assertion regression guard as Inv 10.

### Inv 13 — B/B mode reads `item.json` via python3 (Phase 7c)
Originally asserted that the `rabbit-feature-touch` SKILL.md Step 1 B/B-mode block extracts the related feature from `<item-dir>/item.json` using `python3` (not `jq`, which is not a declared dependency) and references `item.json` (not the legacy `bug.json`). Retired because the entire B/B (bug / backlog) invocation mode was removed from the SKILL.md in the Phase 7c cleanup: with rabbit-file retired in Phase 7b there are no callers passing `--linked-item` to `dispatch-tdd-subagent.py`, so the Step 1 B/B-mode block (and the item.json read path it locks) no longer exists in the SKILL.md. The unified seven-step sequence is now single-mode (normal-mode-only) and resolves scope exclusively via `Skill("rabbit-feature-scope", ...)`.

### Inv 42 — B/B item materialization documented in SKILL.md (Phase 7c)
Originally asserted that the `rabbit-feature-touch` SKILL.md Step 1 contains a `#### B/B item materialization` subsection documenting (a) why materialization is needed (the canonical `bug-backlog-files` branch is never checked out), (b) the local mirror path layout under `.rabbit/rabbit/features/<feature>/<type>s/<id>/`, (c) the `git show origin/bug-backlog-files:...` fetch command, and (d) what gets passed to `--linked-item` (the local mirror directory). Inv 42 also required byte-identical presence of the subsection in both the source and deployed SKILL.md. Retired with Inv 13 — the B/B mode (and the materialization step it required) was removed entirely in the Phase 7c cleanup; with no caller passing `--linked-item`, neither the subsection nor the materialization workflow has a remaining surface to document. The byte-identity requirement between source and deployed SKILL.md is preserved by Inv 1 (build source) and the existing `test_inv41_source_and_deployed_byte_identical` test.
