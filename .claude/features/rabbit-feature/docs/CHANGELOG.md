---
feature: rabbit-feature
owner: rabbit-workflow team
deprecation_criterion: when the rabbit-feature spec's invariant numbering is folded into a structured schema-tracked log
---

# rabbit-feature — Retired Invariants Log

This file holds the tombstones for invariants that were previously declared in `docs/spec.md` and have since been withdrawn. Spec.md no longer carries the prose for these invariants — only a one-line `*(Withdrawn — folded into CHANGELOG.md.)*` marker at the original number.

Each entry below carries the original invariant number (as it appeared in spec.md at the time of retirement), a one-line summary of what the invariant asserted and why it was retired, and the cascade or backlog ID that drove the retirement.

## Layout / structural changes

- **1.46.0 → 1.47.0 — reduction/intent-carrying Step-3 path threads spec-reduction intent into the single TDD cycle (#1198):** Added Inv 64; no invariants retired or renumbered. Background: feature-touch Step 3 invoked rabbit-spec-update and COMMITTED the spec edit BEFORE dispatching the TDD subagent (Step 5). For a housekeep reduction wave the substantive spec edit was thus already committed, so the subagent's `spec-update -> test-red` transition had no working-tree spec diff and was forced to use the `--spec-no-change-reason` escape hatch — a dishonest RED. Its prerequisite #1197 (rabbit-spec v2.9.0) landed an `--intent-only` no-commit mode on rabbit-spec-update that COMPUTES and EMITS the impl-suggestion payload on stdout WITHOUT editing or committing `docs/spec.md`. **Fix (rabbit-feature only, additive/backward-compatible):** the companion `skills/rabbit-feature-touch/scripts/feature-touch.py` gains two subcommands — `is-reduction-wave <request>` (emits JSON `{"reduction": bool}` from the rabbit-housekeep `housekeep: measured reduction wave` signal, so the path DECISION is script-tier per spec-rules.md §4, not judged inline in SKILL.md prose) and `persist-intent <feature-name>` (reads rabbit-spec-update's `--intent-only` payload from stdin and writes it to the same `.rabbit/impl-suggestion-<feature-name>.json` file the Step-5 dispatch already consumes via `dispatch-prompt --impl-suggestion`, editing NOTHING in the feature dir and creating NO commit). SKILL.md Step 3 now documents two paths: the DEFAULT path (unchanged) runs rabbit-spec-update then `commit-spec`; the REDUCTION path runs rabbit-spec-update `--intent-only`, threads the intent via `persist-intent`, and does NOT run `commit-spec`. The intent reaches the TDD subagent through the EXISTING impl-suggestion channel (no new dispatch arg, no tdd-subagent change), so the subagent authors BOTH the spec reduction AND its gating test in ONE honest RED->GREEN cycle on the Step-2 branch/worktree (no double-branching), satisfying the existing `spec-update -> test-red` gate WITHOUT `--spec-no-change-reason`. The tdd-subagent gate (`tdd-step.py`) is NOT modified — rabbit-feature only FEEDS the subagent the intent. **Tests RED→GREEN:** new `test/test-touch-reduction-intent.py` (6 tests): `is-reduction-wave` detects the housekeep signal and is negative for a normal request (FAILED in RED — unknown subcommand); `persist-intent` writes the impl-suggestion file with the verbatim payload and prints its path, and edits/commits NOTHING in the spec (FAILED in RED — unknown subcommand); SKILL.md Step 3 (source AND deployed) documents `--intent-only`, `is-reduction-wave`, `persist-intent` while retaining `commit-spec` for the default path (FAILED in RED — Step 3 had only the single default path); the no-arg usage lists both new subcommands (FAILED in RED). The existing `test-touch-skill*.py`, `test-touch-dispatch-worktree.py`, and `test-version-sync.py` suites still pass unchanged. **Version + lifecycle:** `feature-touch.py` 0.10.0 → 0.11.0; `rabbit-feature-touch` SKILL.md 3.12.0 → 3.13.0; feature/spec/contract version bumped 1.46.0 → 1.47.0 in three-way lockstep (Inv 36). **Deployed republish (dispatcher):** the deployed copy `.claude/skills/rabbit-feature-touch/SKILL.md` changed (Step 3 prose + frontmatter version) and is OUTSIDE the TDD subagent's scope; the subagent republished it in-branch via `contract.lib.publish.publish_skill`, but the dispatcher MUST re-verify/republish before the PR so the deployed-skills-match-source check stays green (Inv 40/41).

- **1.45.0 → 1.46.0 — Step-5 dispatch anchors the TDD subagent at the per-session worktree (#1125, vendored Strategy D):** Added Inv 63; no invariants retired or renumbered. Background: in vendored mode the feature-touch Step 2 create-branch produces a per-session Strategy-D worktree on the feature branch (Inv 61), but Step 5 (dispatch the TDD subagent) had no mechanism to make the subagent execute inside that worktree. The Agent tool has no cwd parameter, so the subagent inherited the host `.rabbit` cwd (on the host's checked-out branch); its scope markers, commits, and state transitions landed on the host tree rather than the per-session worktree's HEAD — defeating Strategy D (#1059) and violating "Never write to main". The cross-feature half — an optional `--worktree <abs>` arg (alias `--cwd`) on `dispatch-tdd-subagent.py` that, when provided, emits absolute worktree-anchored path slots in the dispatch prompt — ALREADY SHIPPED in #1128 (tdd-subagent, Inv 65). This cycle wires rabbit-feature's Step-5 dispatch-argv assembly to PASS that arg. **Fix (rabbit-feature only, option (b) from the issue):** the companion `skills/rabbit-feature-touch/scripts/feature-touch.py` gains a `dispatch-prompt <feature-name> --spec <path> [--impl-suggestion <path>] [--worktree <path>]` subcommand that assembles the `dispatch-tdd-subagent.py` argv. Per spec-rules.md §4 (Script-Backed Orchestration), the computed `--worktree` value and the mode-aware branch live in the SCRIPT, not in a model-assembled SKILL.md bash block. When the create-branch JSON's `worktree` value is present (vendored per-session worktree), the subcommand resolves it to an ABSOLUTE path (the create-branch output may be repo-relative like `.rabbit-worktrees/session-xxxx`) and appends `--worktree <abs>` to the argv. When there is no per-session worktree (standalone, or vendored with an empty/absent worktree value), it appends NO `--worktree` and the emitted argv is byte-identical to the pre-wiring form — a hard back-compat requirement. The `--worktree` argument is OWNED BY tdd-subagent and consumed verbatim; no tdd-subagent file is modified (rabbit-feature's contract `never` block forbids it). The Step-5 SKILL.md body delegates the dispatch-argv assembly to `dispatch-prompt` (no inline control-flow bash; Inv 53/60 preserved) while retaining the `resolve-spec-path` delegation that feeds the `--spec` value. **Tests RED→GREEN:** new E2E `test/test-touch-dispatch-worktree.py` (real subprocess invocations of the companion against a real `dispatch-tdd-subagent.py` and a temp feature spec): vendored-with-worktree assembles an argv containing `--worktree <abs>` resolved to the absolute worktree root (FAILED in RED — subcommand absent); a repo-relative worktree value is resolved to absolute (FAILED in RED); standalone / no-worktree assembles an argv with NO `--worktree`, byte-identical to the pre-wiring argv (FAILED in RED — subcommand absent); the assembled prompt actually runs (exit 0) and, in the worktree case, contains the absolute worktree path in its baked slots (FAILED in RED). The SKILL.md Step 5 delegates to `dispatch-prompt` (FAILED in RED — Step 5 assembled the argv inline). The existing `test-touch-vendored-worktree-cycle.py` and `test-touch-skill*.py` suites still pass unchanged. **Version + lifecycle:** `feature-touch.py` 0.8.0 → 0.9.0; `rabbit-feature-touch` SKILL.md 3.11.0 → 3.12.0; feature/spec/contract version bumped 1.45.0 → 1.46.0 in three-way lockstep (Inv 36). **Deployed republish (dispatcher):** the deployed copy `.claude/skills/rabbit-feature-touch/SKILL.md` changed (Step 5 prose + frontmatter version) and is OUTSIDE the TDD subagent's scope; the subagent republished it in-branch via `contract.lib.publish.publish_skill`, but the dispatcher MUST re-verify/republish before the PR so the deployed-skills-match-source check stays green (Inv 40/41).

- **1.44.0 → 1.45.0 — scaffold-feature.py's plugin/vendored path now creates `test/run.py` (#1114):** No invariants retired or renumbered. Background: `_scaffold_plugin_feature()` (the plugin/vendored scaffold path) wrote `feature.json`, `docs/spec.md`, and `docs/contract.md` but never created `test/run.py`, while the standalone path did. A feature scaffolded via `handoff-scaffold.py` in vendored mode therefore had no `test/` directory, so the feature-touch TDD cycle failed when `tdd-step.py` / the runner tried to invoke the non-existent `test/run.py`. **Fix (rabbit-feature only):** the placeholder runner content was hoisted into a module-level `_RUN_PY` constant plus a `_write_run_py(target)` helper that creates `test/run.py` (executable, exits non-zero until tests are authored). Both `_scaffold_plugin_feature()` AND the standalone path now call this single shared helper, so the two paths emit a byte-identical runner — no divergence. The standalone path's previously-inline runner literal is replaced by the helper call; its `test/` dir creation and all other standalone behavior are unchanged. **Tests RED→GREEN:** new E2E `test/test-scaffold-plugin-test-runpy.py` (3 tests, real subprocess in plugin mode): p1 — non-batch greenfield `<name>` scaffolds an executable `test/run.py` (FAILED in RED — file absent); p2 — non-empty-glob `--batch` scaffolds `test/run.py` (FAILED in RED — file absent); p3 — the plugin-path runner is IDENTICAL to the standalone path's runner (FAILED in RED — plugin file absent). The existing `test/test-scaffold-plugin-greenfield.py` and `test/test-new-feature-scaffolder.py` still pass unchanged. **Version + lifecycle:** `scaffold-feature.py` 2.7.0 → 2.8.0; feature/spec/contract version bumped 1.44.0 → 1.45.0 in three-way lockstep (Inv 36). **Deployed republish:** no deployed surface changed — `scaffold-feature.py` has a single source copy under `features/rabbit-feature/scripts/` (no separate deployed copy), and no SKILL.md body or frontmatter changed — so no dispatcher republish is required.

- **1.43.0 → 1.44.0 — vendored create-branch refuses a toolless worktree and commit-spec refuses a stray host-HEAD spec commit (#1112):** Added Inv 62; no invariants retired or renumbered. Background: Inv 61 (Strategy D, #1086/#1087) assumes the WHOLE `.rabbit/` is tracked so a per-session worktree of the host repo is self-contained. `feature-touch.py create-branch` never VERIFIED that precondition. On a fresh vendored install where `.rabbit/` is present on disk but gitignored and NOT yet committed, `git worktree add -b <branch> <path> HEAD` carried no `.rabbit/` (HEAD has none), producing a TOOLLESS worktree (only README/docs/.gitignore inside) — yet create-branch returned exit 0 with a worktree path that looked fine. The dispatcher, still cwd'd in the original main-tree `.rabbit/`, then ran `commit-spec`, whose `repo_root = _repo_root(Path.cwd())` resolves to the host toplevel and which runs git with cwd=repo_root, so the spec was committed onto the host's shared `main` HEAD with NO guard — silent main pollution, feature branch left empty. **Fix (rabbit-feature only, both scoped to the vendored arm):** (a) `cmd_create_branch`'s vendored arm now precondition-checks `_rabbit_tracked_at_head(repo_root)` (`git ls-tree HEAD .rabbit` non-empty) BEFORE creating the worktree; on failure it writes an actionable message ("commit `.rabbit/` first ... Refusing to build a toolless worktree") to stderr and returns exit 1, emitting NO worktree and NO JSON result. (b) `cmd_commit_spec` now refuses, in plugin/vendored mode, to commit when it is running from the host's MAIN worktree (`_is_main_worktree(repo_root)` — `git rev-parse --git-dir` resolves equal to `--git-common-dir`) WHILE a per-session worktree exists (`_session_worktrees_exist(repo_root)` — a `.rabbit-worktrees/session-*` dir is present); it writes a worktree-pointing message and returns exit 1 before any `git add`/`git commit`. Three new helpers (`_rabbit_tracked_at_head`, `_session_worktrees_exist`, `_is_main_worktree`) back these checks. STANDALONE create-branch/commit-spec are UNCHANGED (no per-session worktree, no untracked-.rabbit precondition; the guard's `mode == "plugin"` clause excludes them). The happy vendored path — committing from INSIDE the per-session worktree (where `--git-dir` != `--git-common-dir`) — still works. **Tests RED→GREEN:** new E2E `test/test-touch-vendored-toolless-worktree.py` (5 tests, real temp git repos + real `git worktree`): vendored create-branch FAILS LOUDLY when `.rabbit/` is untracked and creates no worktree (FAILED in RED — emitted a toolless worktree JSON, exit 0); vendored create-branch still succeeds with a self-contained worktree when `.rabbit/` is tracked (GREEN throughout); vendored commit-spec from the main-tree cwd REFUSES and does NOT move the host HEAD while a per-session worktree exists (FAILED in RED — committed to main, exit 0); vendored commit-spec from INSIDE the worktree still commits onto the feature branch (GREEN throughout); standalone commit-spec is unaffected (GREEN throughout). **Version + lifecycle:** `feature-touch.py` 0.7.0 → 0.8.0; feature/spec/contract version bumped 1.43.0 → 1.44.0 in three-way lockstep (Inv 36). **Deployed republish:** no deployed surface changed — the companion `feature-touch.py` ships in the skill dir and is not a published artifact (no separate deployed copy), and no SKILL.md body or frontmatter changed — so no dispatcher republish is required.

- **1.42.0 → 1.43.0 — scaffold-feature.py honors a `greenfield: true` batch field so a brand-new feature's zero-match globs are no longer mistaken for a typo (#1098):** No invariants retired or renumbered; Inv 45(b) extended in place. Background: `scaffold-feature.py`'s plugin-mode glob path rejected ANY feature whose supplied globs matched zero existing files ("typo guard", scaffold-feature.py ~lines 294-299). That is correct for an EXISTING feature, but WRONG for a GREENFIELD feature being scaffolded: its globs name paths that do not exist yet, so a zero match is expected, not a typo. The guard blocked greenfield scaffolding whenever globs were supplied. The globless greenfield path (empty/absent `globs`) was already exempt (#902), but the with-globs case was not. **Fix (rabbit-feature only):** `_run_plugin_mode` gains a `greenfield: bool = False` parameter; the zero-match typo guard now fires only when `not new_matches and not greenfield`. A greenfield zero-match is accepted and the declared globs are registered verbatim in `project-map.json` (the with-globs analogue of the globless greenfield path). The signal is a new OPTIONAL `greenfield` boolean field on each `--batch` entry (default `false`; a non-boolean value is rejected with a clear error), mirroring how the batch entry already expresses greenfield intent via empty `globs`. The non-batch CLI form has no greenfield flag and so defaults to `greenfield=False` — the typo guard is unchanged there. `scaffold-batch.py` (the skill's thin pass-through) needs no change: `--batch` forwards the JSON file byte-for-byte, so `greenfield` entries flow through unchanged. **Tests RED→GREEN:** new E2E `test/test-scaffold-greenfield-zero-match.py` (3 tests, real subprocess in plugin mode): z1 — a `greenfield: true` batch entry with zero-match globs scaffolds (rc 0) and registers the declared globs in project-map.json (FAILED in RED with the typo-guard error); z2 — a non-greenfield zero-match entry is STILL rejected (rc 1), the guard intact (GREEN throughout); z3 — `greenfield: true` with globs that DO match behaves like a normal registration (GREEN throughout). The existing `test/test-scaffold-plugin-greenfield.py` (globless greenfield + malformed-glob rejection) still passes unchanged. **Version + lifecycle:** `scaffold-feature.py` 2.6.0 → 2.7.0; feature/spec/contract version bumped 1.42.0 → 1.43.0 in three-way lockstep (Inv 36). **Deployed republish:** no deployed surface changed — `scaffold-feature.py` has a single source copy under `features/rabbit-feature/scripts/` (no separate deployed copy), no SKILL.md body changed, and `scaffold-batch.py` is untouched — so no dispatcher republish is required.

- **1.41.3 → 1.42.0 — vendored feature-touch runs the whole TDD cycle in a per-session git worktree (#1087, resolves #1059; child of decomposition parent #1085 Strategy D):** Added Inv 61; no invariants retired or renumbered. Background: in vendored mode the feature-touch git ops operated on the HOST repo's SINGLE shared HEAD, so two concurrent sessions stomped each other — session A's create-branch then session B's checkout moved the shared HEAD (#1059). The earlier #1059 attempt (#1082) found this incoherent under Strategy A because A kept tool and work SPLIT. Under Strategy D (#1085) the WHOLE `.rabbit/` is tracked in the host repo (shipped by the now-merged #1086, rabbit-cage), making a worktree of the host repo SELF-CONTAINED — it holds both the tool (`.rabbit/.claude`) AND the work (`.rabbit/rabbit-project`) at consistent paths, exactly like a standalone worktree. So the proven STANDALONE worktree machinery works unchanged in vendored mode. **Fix (rabbit-feature only):** the companion `skills/rabbit-feature-touch/scripts/feature-touch.py` `create-branch` subcommand is now mode-aware and emits a single JSON line `{"branch", "worktree", "mode"}`. STANDALONE keeps the existing `git checkout -b <branch>` in the current repo (which already owns a dedicated HEAD) with `worktree: null`. PLUGIN/VENDORED (dual-accepting `vendored` and legacy `plugin` mode-marker values via the existing `_mode()`) creates a PER-SESSION git worktree of the host repo via `git worktree add -b <branch> <path> HEAD`, placed OUTSIDE the tracked tree at `<host>/.rabbit-worktrees/session-<token>/` (NEVER under `.rabbit/`), and emits that path; each session thereby gets its OWN HEAD, so concurrent vendored sessions never stomp the shared HEAD (resolves #1059). The cycle's remaining steps run from `<worktree>/.rabbit`. This COLLAPSES the vendored VCS path toward the standalone worktree machinery (a single `git worktree` mechanism) rather than a parallel implementation, per the maintainer's fewer-modes preference. **No cross-scope change:** no tdd-subagent dispatch-contract change is required — `dispatch-tdd-subagent.py` resolves `--spec` cwd-relative inside the self-contained worktree (#1085's acceptance; #1088 verifies this separately). The SKILL.md Step 2 documents the mode-aware behaviour in prose and continues to delegate the computed branch-name + VCS work to the companion script (no inline control-flow bash; Inv 53/60 preserved). **Tests RED→GREEN:** new E2E `test/test-touch-vendored-worktree-cycle.py` (6 tests, real temp git repos + real `git worktree`): vendored create-branch makes an isolated per-session worktree on the feature branch WITHOUT moving the host HEAD; the worktree is OUTSIDE `.rabbit/`; the worktree is self-contained (tool + work present at the same relative paths); a full vendored cycle (spec-commit + impl-commit) lands BOTH commits on the feature branch while the host HEAD stays at base; the #1059 regression — two concurrent vendored sessions get DISTINCT worktrees/branches and interleaved commits never leak across sessions or move the host HEAD; and standalone create-branch is UNCHANGED (plain `git checkout -b`, no worktree, `worktree: null`). All vendored cases FAILED in RED (create-branch emitted a bare branch name, not JSON, and created no worktree), green after. **Version + lifecycle:** `feature-touch.py` 0.6.0 → 0.7.0; `rabbit-feature-touch` SKILL.md 3.10.0 → 3.11.0; feature/spec/contract version bumped 1.41.3 → 1.42.0 in three-way lockstep (Inv 36). **Deployed republish (dispatcher):** the deployed copy `.claude/skills/rabbit-feature-touch/SKILL.md` changed (Step 2 prose + frontmatter version) and is OUTSIDE the TDD subagent's scope; the subagent republished it in-branch via `contract.lib.publish.publish_skill`, but the dispatcher MUST re-verify/republish before the PR so the deployed-skills-match-source check stays green (Inv 40/41).

- **1.41.2 → 1.41.3 — `/rabbit-tdd-autonomous` writes the `.rabbit-tdd-autonomous` marker at the GIT TOPLEVEL, not inside `.rabbit` (#1072):** No invariants retired, added, or renumbered. Background: #1048 (contract Inv 68) made the READER `contract.lib.runtime._repo_markers_root` resolve a marker-file configurable's repo-root marker at the git toplevel — `dirname(RABBIT_ROOT)` when vendored (RABBIT_ROOT is the `.rabbit` install dir), `RABBIT_ROOT` unchanged when standalone. But the WRITE command `scripts/rabbit-tdd-autonomous-config.py` passed its `repo_root()` (which returns `RABBIT_ROOT` when that env var is set) straight to `dispatch_config`, whose `write_marker`/`delete_marker` primitives join the path against that root. In a vendored Claude Code session `RABBIT_ROOT` points at the `.rabbit` install dir, so the marker was WRITTEN at `<RABBIT_ROOT>/.rabbit-tdd-autonomous` (inside `.rabbit`) — one `.rabbit` deeper than the new git-toplevel reader looks. Write and read disagreed in vendored mode; the Step-4 bypass set via `/rabbit-tdd-autonomous true` was never observed. **Fix (write side, rabbit-feature only):** a new `markers_root(rroot)` helper computes the git toplevel exactly as the reader does — `dirname(rroot)` when vendored, `rroot` when standalone — detecting vendored via rabbit-meta's canonical `detect_mode`/`is_vendored` (lazy-imported from `<rroot>/.claude/features/rabbit-meta/lib/mode_detection.py`) with the same `basename(rroot) == ".rabbit"` fallback the reader uses. The framework root (`rroot` = RABBIT_ROOT) still loads `feature.json` and imports `contract.lib`; only the marker write/delete now targets `markers_root(rroot)`. Standalone is unchanged (toplevel == repo_root there already). The reader (`contract.lib.runtime`) and the contract Inv 68 are NOT touched — they were already correct. **Tests RED→GREEN:** `test/test-tdd-autonomous-vendored-marker-root.py` (e2e, new) runs the real script as a subprocess against a simulated vendored layout (RABBIT_ROOT = a `.rabbit` dir whose parent is the toplevel) and asserts the marker lands at the toplevel and NOT inside `.rabbit` (both FAILED in RED), plus a standalone regression case (GREEN throughout). The existing `test/test-tdd-autonomous-command.py` (tmp dir not named `.rabbit` → standalone) still passes unchanged. **Version + lifecycle:** feature/spec/contract version bumped 1.41.2 → 1.41.3 in three-way lockstep (Inv 36). **Deployed republish:** no deployed surface changed (the command `.md` is unchanged and already in sync; the backing script is invoked in place under `features/rabbit-feature/scripts/` with no separate deployed copy), so no dispatcher republish is required.

- **1.41.1 → 1.41.2 — vendored `resolve-spec-path` emits a CWD-RELATIVE path so the consumer finds the spec (#1061):** No invariants retired, added, or renumbered; Inv 54 sub-points (a)/(b) description extended in place. Background: in a vendored install the rabbit session cwd IS the `.rabbit/` install dir, while `feature-touch.py._repo_root()` walks up to the HOST git toplevel (the parent of `.rabbit/`). The producer `resolve-spec-path` emitted the spec path relative to that host `repo_root`, i.e. `.rabbit/rabbit-project/features/<name>/docs/spec.md`. But the CONSUMER, `tdd-subagent/scripts/dispatch-tdd-subagent.py`, validates/resolves its `--spec` argument relative to its CURRENT WORKING DIRECTORY (the `.rabbit/` dir), so it looked for `.rabbit/.rabbit/rabbit-project/.../spec.md` and failed (`ERROR: --spec file does not exist`, exit 2). The two scripts disagreed on the emission base in vendored mode. **Fix (producer side, rabbit-feature only):** `resolve-spec-path` / `resolve-contract-path` now emit the resolved path relative to a mode-aware base via a new `_emit_base(repo_root, mode)` helper — STANDALONE: `repo_root` (unchanged; repo_root == cwd there); PLUGIN/VENDORED: `Path.cwd()`. In a vendored install run from `.rabbit/` this strips the leading `.rabbit/` so the emitted path is `rabbit-project/features/<name>/docs/spec.md`, which the consumer resolves correctly from its `.rabbit/` cwd. The convention reproduces rabbit-cage `lib/runtime_root.rabbit_runtime_root`'s single-`.rabbit` semantics (#1046) without adding a cross-feature runtime import to a shipped companion script. `commit-spec` is untouched (it resolves the spec relative to `repo_root` for its `git diff`/`git add`, which is correct). The consumer side (`dispatch-tdd-subagent.py`) was OUT OF SCOPE and not modified. **Tests RED→GREEN:** `test/test-touch-docs-resolver.py` gains `test_resolve_spec_vendored_cwd_relative_no_rabbit_prefix`, `test_resolve_spec_vendored_legacy_plugin_marker_cwd_relative`, `test_resolve_contract_vendored_cwd_relative_no_rabbit_prefix` (vendored layout with cwd = `.rabbit/` → emits a cwd-relative path with NO `.rabbit/` prefix that resolves to a real file from cwd; all three FAILED in RED with the host-root-relative `.rabbit/...` path), plus `test_resolve_spec_standalone_unchanged_by_1061` (standalone regression, GREEN throughout). The existing `test_resolve_spec_plugin_mode_flat_docs` (cwd = host root) still passes, confirming the cwd-base rule preserves the host-cwd plugin scenario. **Version + lifecycle:** `feature-touch.py` 0.5.0 → 0.6.0; `test/test-touch-docs-resolver.py` 2.1.0 → 2.2.0; feature/spec/contract version bumped 1.41.1 → 1.41.2 in three-way lockstep (Inv 36). **Deployed republish:** no deployed surface changed (the companion script ships in the skill dir and is not a published artifact; no SKILL.md body changed), so no dispatcher republish is required.

- **1.41.0 → 1.41.1 — feature-touch.py `_mode()` dual-accepts the `vendored` content value (#1045):** No invariants retired, added, or renumbered; Inv 54 mode-detection description extended in place. Background: rabbit-meta's `detect_mode` was migrated (#980) to emit the literal `vendored` as the current synonym for the legacy `plugin` mode-marker content, and `scaffold-feature.py` already dual-accepts both via `_VENDORED_MODES` (Inv 44). The companion `feature-touch.py._mode()` was NOT migrated — it returned `"plugin"` only when `<repo_root>/.rabbit/.runtime/mode` contained exactly `plugin`. In a current `vendored` install this made `resolve-spec-path` / `resolve-contract-path` resolve under the standalone `.claude/features/<name>/` prefix and made `commit-spec` `git add` (no `-f`) a nonexistent standalone dir, hit the empty-diff skip, and silently NOOP — so the project's decomposed spec/impl under the gitignored `.rabbit/rabbit-project/features/<name>/` was never force-staged and nothing was git-trackable (the #1045 symptom). **Fix:** `_mode()` now dual-accepts both `vendored` and `plugin` (a `_VENDORED_MODES` tuple mirroring scaffold-feature.py), selecting the plugin feature-dir prefix and the `git add -f` staging form for either value. This is the bounded in-scope resolution of #1045: because plugin mode already force-stages with `git add -f`, the project's own decomposed work commits with NO host `.gitignore` change required — option (A)/(B) from the issue is moot for the project-artifact path once mode is detected correctly. **Tests RED→GREEN:** `test/test-touch-docs-resolver.py` gains `test_resolve_spec_vendored_mode_flat_docs` (vendored marker → resolves under `.rabbit/rabbit-project/features/`) and `test_commit_spec_vendored_mode_force_adds_gitignored` (vendored marker + `.gitignore` ignoring `.rabbit/` → `commit-spec` force-stages and commits the gitignored vendored spec; reproduces the silent-NOOP bug in RED). **Version + lifecycle:** `feature-touch.py` 0.4.0 → 0.5.0; feature/spec/contract version bumped 1.41.0 → 1.41.1 in three-way lockstep (Inv 36). **Deployed republish:** no deployed surface changed (the companion script ships in the skill dir and is not a published artifact; no SKILL.md body changed), so no dispatcher republish is required.

- **1.40.0 → 1.41.0 — rabbit-feature-scaffold hands off to the rabbit-spec-creator subagent directly (#922, piece 5/5):** No invariants retired, added, or renumbered. Background: the `rabbit-spec-create` SKILL wrapper is retired across the workflow (#922). The `rabbit-spec-creator` SUBAGENT now writes its own `docs/spec.md` and is dispatched DIRECTLY (level-1); rabbit-spec's input assembler was renamed `dispatch-spec-create.py` → `dispatch-spec-creator.py`. This cycle (rabbit-feature scope, the final source piece) retargets every spec-create reference in the rabbit-feature-scaffold plugin-mode handoff. **Source changes:** `scripts/scaffold-feature.py` — both plugin-mode `NEXT:` blocks (greenfield + globbed) and the placeholder spec body / comments now name `dispatch-spec-creator.py` and instruct the caller to dispatch `Agent(subagent_type: "rabbit-spec-creator", ...)` directly; the script still never dispatches the subagent itself (no Agent/Skill coupling). `skills/rabbit-feature-scaffold/SKILL.md` — Step 1 NEXT-block description, Step 3 (retitled "Dispatch rabbit-spec-creator"), Step 4 report, and the "What You Do NOT Do" example rewritten to the direct-dispatch model. `docs/spec.md` — Inv 46(b), Inv 48 (retitled "Plugin-mode spec-creator dispatch handoff"), and Inv 49 retargeted. `docs/contract.md` — `invokes.scripts` renamed to `dispatch-spec-creator.py`; added an `invokes.agents` entry for the `rabbit-spec-creator` subagent. `feature.json` summary reworded (spec lifecycle = the rabbit-spec-creator subagent + rabbit-spec-update). **Tests RED→GREEN:** `test/test-new-skill.py` (SKILL.md names dispatch-spec-creator.py + rabbit-spec-creator, not the retired refs); `test/test-feature-new-plugin-mode.py` (the E2E plugin-mode run asserts the printed `NEXT:` block names dispatch-spec-creator.py + rabbit-spec-creator and no retired ref); new guard `test/test-spec-deadweight-removed.py::test_no_retired_spec_create_reference` (no rabbit-feature surface — spec.md, contract.md, SKILL.md, scaffold-feature.py — references the retired `rabbit-spec-create` skill or the old `dispatch-spec-create.py` name; the live handoff surfaces name the new path). **Version + lifecycle:** `rabbit-feature-scaffold` SKILL.md 1.9.0 → 1.10.0; feature/spec/contract version bumped 1.40.0 → 1.41.0 in three-way lockstep (Inv 36). **Deployed republish (dispatcher):** the deployed copy `.claude/skills/rabbit-feature-scaffold/SKILL.md` changed and is OUTSIDE the TDD subagent's scope; the dispatcher MUST republish it before the single #922 PR so the deployed-skills-match-source check stays green. **Barrier note:** this is piece 5/5 of the #922 barrier; the cross-feature contract gate goes green only AFTER the dispatcher syncs deployed surfaces.

- **1.39.0 → 1.40.0 — rabbit-feature-scaffold becomes the declared interface for BOTH single and batch scaffolding (#921, piece 1/2):** No invariants retired, added, or renumbered. Background: in plugin-mode decompose, `rabbit-decompose` shelled out to `scaffold-feature.py --batch` directly, bypassing the `rabbit-feature-scaffold` skill — a layering violation (the skill is the user-facing scaffold primitive). Piece 1 (this cycle, rabbit-feature scope) adds an explicit batch surface AT THE SKILL LAYER so the skill, not a raw script shell-out, is the declared interface for both modes. **New companion script** `skills/rabbit-feature-scaffold/scripts/scaffold-batch.py` (spec-rules.md §4 Script-Backed Orchestration — the batch/normalization logic lives in a script, not in model-assembled SKILL.md bash): `--batch <features.json>` passes a JSON array file straight to `scaffold-feature.py --batch`; `--list "<name> [glob ...]; ..."` normalizes a `;`-separated inline batch into the same JSON-array shape then delegates; a bare `<name> [glob ...]` delegates byte-for-byte to `scaffold-feature.py`'s existing single-feature surface. The single-feature invocation surface and all existing behaviour are PRESERVED — `scaffold-feature.py` itself is unchanged (it already supported `--batch`). SKILL.md documents both the single and batch surfaces, names the companion script as the boundary callers use, and the `description` field gains the batch trigger phrasing. **New E2E guard:** `test/test-scaffold-skill-batch-mode.py` (5 tests): companion exists+executable (b0), JSON-file batch delegates and scaffolds every entry (b1), inline `--list` batch delegates (b2), single mode preserved (b3), SKILL.md documents the batch surface + references the companion (b4). RED→GREEN confirmed: all 5 failed before implementation (missing companion / undocumented surface), green after. **Version + lifecycle:** `scaffold-batch.py` 1.0.0 (new); `rabbit-feature-scaffold` SKILL.md 1.8.0 → 1.9.0 (description + Inputs + Protocol document batch); feature/spec/contract version bumped 1.39.0 → 1.40.0 in three-way lockstep (Inv 36). **Deployed republish (dispatcher):** the deployed copy `.claude/skills/rabbit-feature-scaffold/SKILL.md` changed and is OUTSIDE the TDD subagent's scope; the dispatcher MUST republish it (piece 2 runs the contract gate before the single PR) so the deployed-skills-match-source check stays green. The companion script is NOT a published artifact (it ships in the skill dir, like `feature-touch.py`). **Barrier note:** piece 2 (rabbit-decompose scope, same branch) updates `rabbit-decompose`'s `handoff-scaffold.py` to invoke this companion script instead of shelling out to `scaffold-feature.py --batch` directly, then opens the single PR.

- **1.38.0 → 1.39.0 — make plugin-mode scaffold symmetric with standalone for greenfield (globless) features (#902):** No invariants retired or added; Inv 44, 45, 46, 47, 49 reworded in place. `scaffold-feature.py` was asymmetric between modes: STANDALONE allowed scaffolding a feature with NO globs (greenfield), but PLUGIN mode REQUIRED a non-empty glob list in BOTH the `--batch` form (`batch[i].globs must be a non-empty list of strings`) and the non-batch form (`if len(args) < 2: return 2`), so there was no way to scaffold a globless greenfield feature in a plugin install — which broke rabbit-decompose's greenfield scenario. **Fix (three relaxations):** (1) `_run_plugin_mode` now handles an empty `globs` list by scaffolding the feature dir + `feature.json` (`paths: []`) WITHOUT a project-map glob registration — the project-map schema requires a non-empty `paths` list per registered feature, so a globless feature is simply omitted from the map; it mirrors standalone's no-glob path. (2) The `--batch` validation accepts an empty/absent `globs` list (still rejects a present-but-malformed `globs` that is not a list of strings). (3) The non-batch arg guard accepts a bare `<name>` with no globs (`len(args) < 1`). Non-empty-glob behavior (resolution, boundary/empty-match/overlap validation, project-map registration) is unchanged. **New E2E guard:** `test/test-scaffold-plugin-greenfield.py` (4 tests): empty-batch-globs scaffolds greenfield with no project-map entry (g1), non-batch bare `<name>` scaffolds greenfield (g2), non-empty-glob batch still registers project-map (g3, regression), and genuinely-malformed globs (string-not-list, list-of-non-strings) still rejected (g4). RED→GREEN confirmed: g1/g2 failed before the fix (rc 1 / rc 2), green after. **Version + lifecycle:** `scaffold-feature.py` 2.3.0 → 2.4.0; `rabbit-feature-scaffold` SKILL.md 1.7.0 → 1.8.0 (Inputs + plugin synopsis document optional globs); feature/spec/contract version bumped 1.38.0 → 1.39.0 in three-way lockstep (Inv 36). **Deployed republish (dispatcher):** the deployed copy `.claude/skills/rabbit-feature-scaffold/SKILL.md` changed and is OUTSIDE the TDD subagent's scope; the dispatcher MUST republish it so the contract suite's deployed-skills-match-source check stays green. **skill-creator validation note:** the SKILL.md `description` field is unchanged, so triggering accuracy is preserved; the interactive skill-creator pass is not invocable from inside a TDD subagent cycle.

- **1.37.0 → 1.38.0 — resolve 8 script-backed-orchestration findings in rabbit-feature (#873, child of #863):** Added Inv 60. `check-script-backed.py scan .claude/features/rabbit-feature/` reported 8 `runtime-placeholder` findings across the three SKILL bodies; each was dispositioned per `spec-rules.md` §4 verify-or-flag. SEVEN are illustrative usage/synopsis snippets that document HOW to invoke a script (its argument shape) rather than a live model-assembled orchestration step — each was marked with the `<!-- example -->` exemption marker (the non-executable-documentation mechanism shipped in #869's `check-script-backed.py`) on the line directly above its opening fence: `rabbit-feature-scaffold` standalone/plugin `scaffold-feature.py` synopses + the `validate-feature.py` shim synopsis; `rabbit-feature-scope` Step 1 `resolve-scope.py` synopsis; `rabbit-feature-touch` Step 3 `commit-spec` synopsis, Step 5 `resolve-spec-path` dispatch-wiring synopsis, and Step 7 `gh pr create` shape (title/body are free-form model-authored prose, not a script-computable value). ONE finding was a genuine prompt-tier step and was CONVERTED, not exempted: `rabbit-feature-touch` Step 2 branch creation assembled a deterministic `feat/<feature-name>[-multi]-<keywords>` branch name from the request and `git checkout -b`'d it inline; its computation now lives in the companion `feature-touch.py create-branch [--multi] <feature-name> <request>` subcommand (the script derives `<keywords>` = first 2–4 alnum request words, assembles the branch name, checks it out, and prints it), and the SKILL Step 2 invokes the subcommand. After the change the scanner reports `{"count": 0}`. New E2E regression test `test/test-script-backed-clean.py` runs the scanner against the feature and asserts count 0, asserts Step 2 invokes `create-branch` (and no inline `git checkout -b <branch-name>` remains), and E2E-exercises `create-branch` (single + `--multi`) in a temp git repo. Companion `feature-touch.py` version bumped 0.3.0 → 0.4.0; SKILL.md frontmatter versions bumped (`rabbit-feature-touch` 3.9.0 → 3.10.0, `rabbit-feature-scope` 1.1.0 → 1.2.0, `rabbit-feature-scaffold` 1.6.0 → 1.7.0); feature/spec/contract version bumped 1.37.0 → 1.38.0 in three-way lockstep (Inv 36). **Deployed republish (dispatcher):** all three deployed copies (`.claude/skills/rabbit-feature-touch/SKILL.md`, `.claude/skills/rabbit-feature-scope/SKILL.md`, `.claude/skills/rabbit-feature-scaffold/SKILL.md`) changed and are OUTSIDE the TDD subagent's scope; the dispatcher MUST republish them so the contract suite's deployed-skills-match-source check stays green. **skill-creator validation note:** the `skill-creator:skill-creator` meta-skill is interactive and cannot be invoked from inside a TDD subagent cycle, so the full skill-creator pass is DEFERRED to a follow-up; no SKILL.md `description` field changed, so triggering accuracy is preserved.

- **1.36.0 → 1.37.0 — retire the rabbit-feature-audit skill; auditing is run directly via contract's validate-feature.py (#853, piece 2/2):** Retired Inv 34 and Inv 35 (the `rabbit-feature-audit` SKILL.md invocation-surface and `validate-feature.py`-invocation invariants); the `### rabbit-feature-audit SKILL.md` section is gone. The skill's deprecation criterion ("validate_feature exposed via a first-class CLI in the contract feature") is now fully met: contract's `scripts/validate-feature.py` exists and (via piece 1, contract v2.32.0) supports the `all` cross-feature sweep the skill provided. Auditing is therefore run directly as `validate-feature.py <feature-dir>|all` (script-tier, owned by contract). REMOVED: the skill source dir `skills/rabbit-feature-audit/` and its skill test `test/test-audit-skill.py`; the audit entry from `feature.json` `surface.skills` + `manifest`; the `provides.skills` audit entry and audit references in `docs/spec.md` / `docs/contract.md`. SURVIVES: the standalone team-owner enforcement script `scripts/audit-owner.py` (Inv 50) — it enforces a rule (`owner == "rabbit-workflow team"`) that `validate_feature` does NOT cover, so it is re-homed in spec from "skill-wired" to "run directly (script-tier)"; `test/test-audit-owner.py` keeps its script-behaviour tests and drops the two now-dead SKILL.md-wiring tests. **New E2E guard:** extended `test/test-spec-deadweight-removed.py` with `test_audit_skill_retired` (skill dir gone; no audit reference in `surface.skills`, `manifest`, `provides.skills`, or live surfaces) and `test_audit_owner_script_survives` (the standalone script remains executable). RED→GREEN confirmed: the retirement guard failed on the present skill dir before implementation, green after. **Version + lifecycle:** feature/spec/contract version bumped 1.36.0 → 1.37.0 in three-way lockstep (Inv 36); `test-audit-owner.py` docstring version 1.0.0 → 1.1.0 and `test-manifest-deploys-correctly.py` 1.0.0 → 1.1.0 for the reframed docstrings. **Deployed teardown (dispatcher):** the deployed copy `.claude/skills/rabbit-feature-audit/` is OUTSIDE the TDD subagent's scope; the dispatcher MUST remove it (republish/teardown) so the contract suite's deployed-skills-match-source check stays green.
- **1.35.0 → 1.36.0 — housekeeping: measured restated-rationale removal from doc surfaces (#812, under #794):** No invariants retired, added, or renumbered. A measured verify-or-flag reduction wave (coding-rules §6, §2, §7) cut restated rationale/history from the live doc surfaces while preserving every load-bearing token, gated literal, and behavioural spec line. Removals: (1) spec.md Inv 44 — the "walk-up replaces the original single-check semantics" backstory (the walk-up behaviour itself stays specified; the history lives in `test/test-scaffold-feature-detects-plugin-mode-walk-up.py`'s docstring); (2) spec.md Inv 56 — the "dead `specs/` fallback is removed: every feature has migrated..." backstory (the behavioural spec — prefer flat `docs/`, fall back to legacy `docs/spec/`, default to flat — stays); (3) `rabbit-feature-touch` SKILL.md Step 3 — collapsed the duplicated spec-commit rationale (the trailing "This prevents spec edits from falling through uncommitted" paragraph and the restated mode-aware enumeration) while keeping all Inv 16/53 gated literals ("Commit spec changes BEFORE Step 5", the commit-message pattern, the empty-diff skip, the `feature-touch.py commit-spec` invocation, the §4 citation); Step 5 §4 prose tightened (the `resolve-spec-path` delegation + citation kept); (4) `rabbit-feature-audit` SKILL.md — dropped the Notes bullet that verbatim-restated the Protocol intro's "change the library, not this skill"; (5) `rabbit-feature-scaffold` SKILL.md — dropped the Notes bullet that re-named the scaffolder path already given twice in Step 1. **Measured delta (doc surfaces):** spec.md 580→577, `rabbit-feature-touch` SKILL.md 219→207, `rabbit-feature-audit` SKILL.md 101→98, `rabbit-feature-scaffold` SKILL.md 163→162 — 19 lines removed across doc surfaces. The feature was already heavily housekept (strict-tier opted-in via `housekeeping_clean`; prior waves #639/#685/#700/#717), so the honest reduction is modest. **New E2E guard:** extended `test/test-spec-deadweight-removed.py` with `test_live_surfaces_no_restated_rationale`, asserting the removed history fragments cannot regrow on the live surfaces (RED→GREEN confirmed: 2 fragments present before, 0 after). **Version + lifecycle:** feature/spec/contract version bumped 1.35.0 → 1.36.0 in three-way lockstep (Inv 36); `rabbit-feature-touch` SKILL.md 3.8.0 → 3.9.0, `rabbit-feature-audit` SKILL.md 1.2.0 → 1.3.0, `rabbit-feature-scaffold` SKILL.md 1.5.0 → 1.6.0 for the body edits (deployed copies republished in-branch).
- **1.34.0 → 1.35.0 — drop rabbit-config coexistence assertions ahead of retirement (#769):** No invariants retired, added, or renumbered; Inv 58 reworded in place. As part of the atomic retirement of the `rabbit-config` feature (#769, children #781/#782/#783), rabbit-feature drops every now-obsolete live-surface reference to the central `/rabbit-config tdd-autonomous` interpreter that would break once the rabbit-config dir is removed. The coexistence window for this configurable has ended: `/rabbit-tdd-autonomous` (this feature's per-feature command, #767) is the SOLE supported surface. **E2E guard change:** `test/test-tdd-autonomous-command.py` drops case (vi) — the coexistence assertion that ran the rabbit-config interpreter at `.claude/features/rabbit-config/skills/rabbit-config/scripts/rabbit-config.py` — along with the `RABBIT_CONFIG` constant; the `/rabbit-tdd-autonomous` command tests are intact. `test/test-tdd-autonomous-alert.py` was already validated through rabbit-feature's OWN per-feature `check_marker_alert` runtime entry (Inv 59, via `contract.lib.runtime`); only its docstring is reframed to drop the live central-enumeration comparison. **Doc surfaces:** Inv 58 in `docs/spec.md`, the `provides.commands` purpose + `manages.runtime_markers` writer in `docs/contract.md`, and the `commands/rabbit-tdd-autonomous.md` body all reworded from "coexists with the central /rabbit-config surface" to "sole supported surface; coexistence window ended (#769)". The marker references (`.rabbit-tdd-autonomous` canonical + the legacy `.rabbit-human-approval-bypass` dual-read) are untouched. **Version + lifecycle:** feature/spec/contract version bumped 1.34.0 → 1.35.0 in three-way lockstep (Inv 36); `commands/rabbit-tdd-autonomous.md` bumped 1.0.1 → 1.0.2 for the descriptor reword (deployed copy republished in-branch). No `rabbit-feature-touch` SKILL.md body changed. This is a barrier-intermediate change: it removes only obsolete coexistence assertions, so the rabbit-feature suite and the contract gate stay GREEN while rabbit-config is still present; a later barrier subagent removes the rabbit-config dir itself.
- **1.33.0 → 1.34.0 — migrate Step-4 config refs to /rabbit-tdd-autonomous (phase 4 of #733, issue #768):** No invariants retired, added, or renumbered; Inv 8, 9, 12 reworded in place. The `rabbit-feature-touch` SKILL.md Step 4 (Human Approval) is migrated off the retired central `/rabbit-config human-approval true|false` instruction and the old `human-approval` configurable name onto the per-feature command `/rabbit-tdd-autonomous true|false` (owned by rabbit-feature, manifested in phase 3) with CORRECT post-#336 polarity: `true` writes the canonical bypass marker `.rabbit-tdd-autonomous` (autonomous/bypass ACTIVE) and `false` (default) removes it (Step-4 gate ACTIVE). The bypass-check now names the canonical marker `.rabbit-tdd-autonomous` (the legacy `.rabbit-human-approval-bypass` remains dual-read by `dispatch-tdd-subagent.py` per #770, documented as coexistence). The Step-4 bypass-active alert is now sourced from rabbit-feature's OWN `tdd-autonomous` configurable via `emit_configurable_alert('rabbit-feature', 'tdd-autonomous', ...)` instead of the cross-scope `('rabbit-cage', 'human-approval')` read (the configurable was relocated to its owning feature in phase 3). Inv 8/9/12 in `docs/spec.md` reworded to the canonical marker + command + owning-feature alert source. Remaining bare `human-approval` descriptive mentions across rabbit-feature live surfaces (contract.md purpose, command body, config-script + test docstrings) reframed to "tdd-autonomous"/"approval"; the only surviving `human-approval` / `/rabbit-config human-approval` strings are the legacy dual-read marker name (coexistence) and the NEGATIVE-assertion regression guard in `test/test-touch-skill.py`. The retired `--human-approval-gate` CLI-flag regression guard is unaffected (different artifact). **E2E guards:** `test/test-touch-skill.py` MARKER_PATH/management-command/alert-routing assertions retargeted to the canonical marker + `/rabbit-tdd-autonomous` + `('rabbit-feature','tdd-autonomous')`, plus a new negative guard `test_inv8_step_4_no_stale_config_refs` asserting Step 4 carries no `/rabbit-config human-approval` or bare `human-approval`. **Version + lifecycle:** feature/spec/contract version bumped 1.33.0 → 1.34.0 in three-way lockstep (Inv 36); the `rabbit-feature-touch` source SKILL.md frontmatter version bumped 3.7.0 → 3.8.0 (deployed copy must be republished by the dispatcher), and `commands/rabbit-tdd-autonomous.md` bumped 1.0.0 → 1.0.1 for the descriptor reword.
- **1.32.0 → 1.33.0 — manifest /rabbit-tdd-autonomous + relocate configurable here (phase 3 of #733):** No invariants retired or renumbered; three added (Inv 57, 58, 59). The `tdd-autonomous` configurable — the human-approval gate over the feature-touch TDD cycle Step 4 — is relocated to its owning feature (out of `rabbit-cage`, which dropped it in the same barrier PR) and re-declared in `rabbit-feature`'s `feature.json configuration[]` (Inv 57). Post-#336 polarity: `default false` keeps the Step-4 gate ACTIVE (no bypass marker); `true` writes the NEW canonical bypass marker `.rabbit-tdd-autonomous`, `false` deletes it. `rabbit-feature` now manifests the THIN `/rabbit-tdd-autonomous true|false` command (`commands/rabbit-tdd-autonomous.md` + `scripts/rabbit-tdd-autonomous-config.py`, a wrapper over `contract.lib.config_dispatch.dispatch_config` — no re-implemented interpreter) via a `publish_command` manifest entry (Inv 58), and declares a PER-FEATURE Stop + SessionStart `check_marker_alert` runtime entry for the active bypass marker (Inv 59), re-homing the override alert from rabbit-config's central `iterate_configurables_*`. COEXISTENCE (#769): `/rabbit-config tdd-autonomous` still mutates the configurable (rabbit-config enumerates all features' `configuration[]`); rabbit-config is NOT retired here. The Step-4 gate consumer `dispatch-tdd-subagent.py` dual-reads both `.rabbit-human-approval-bypass` and `.rabbit-tdd-autonomous` (#770), so the new canonical marker is honored. Doc-reference migration of the SKILL Step-4 prose pointing at `/rabbit-config` is deferred to #768; the `rabbit-feature-touch` SKILL.md gained only a minimal one-line pointer naming the new command alongside the existing one. Added `.rabbit-tdd-autonomous` to `.gitignore`. **New E2E guards:** `test/test-tdd-autonomous-configurable.py` (Inv 57), `test/test-tdd-autonomous-command.py` (Inv 58 — runs the real script subprocess against a temp repo; also asserts /rabbit-config coexistence), `test/test-tdd-autonomous-alert.py` (Inv 59 — drives the declared runtime alert through `contract.lib.runtime`). `test/test-manifest-shape.py` (Inv 40) relaxed to allow `publish_command` entries alongside the one-per-skill `publish_skill` set. **Version + lifecycle:** feature/spec/contract version bumped 1.32.0 → 1.33.0 in three-way lockstep (Inv 36); the `rabbit-feature-touch` source SKILL.md frontmatter version bumped 3.6.0 → 3.7.0 for the minimal pointer (deployed copy must be republished by the dispatcher).
- **1.31.0 → 1.32.0 — housekeeping: correct dead `spec-seeder` references in `docs/spec.md` (issue #717, under #639):** No invariants retired, added, or renumbered. `docs/spec.md` carried dead references to the absorbed `spec-seeder` feature on five lines (Inv 46 sub-bullet, Inv 48 title + body, Inv 49 body). PROVE-IT-DEAD verification per #639: `find .claude/features/spec-seeder` is EMPTY (the feature was absorbed into `rabbit-spec`); `grep` for `dispatch-spec-seeder` found zero live callers under `.claude/features/`; the live `scaffold-feature.py` already prints a `NEXT:` line naming `Skill("rabbit-spec-create", ...)` and the `.claude/features/rabbit-spec/scripts/dispatch-spec-create.py` dispatcher (the `spec-creator` subagent). The dead references were therefore stale spec prose lagging the live implementation. CORRECTED (reword to live owner, not deletion — the plugin-mode handoff still applies): Inv 48 retitled "Plugin-mode spec-create dispatch handoff" and retargeted to `dispatch-spec-create.py` at `.claude/features/rabbit-spec/scripts/`; the Inv 46 placeholder bullet and Inv 49 two-step flow now name the `spec-creator` subagent via the `rabbit-spec-create` skill. Also corrected a stale `spec-seeder` comment in `test/test-feature-new-plugin-mode.py` (the assertion already checked the live `dispatch-spec-create.py`). **New E2E guard:** extended `test/test-spec-deadweight-removed.py` with `test_no_spec_seeder_reference`, asserting the `spec-seeder` feature dir is absent and that no live surface (`docs/spec.md`, `docs/contract.md`) names `spec-seeder`, `dispatch-spec-seeder.py`, or `.claude/features/spec-seeder`. RED→GREEN confirmed: 5 dead lines before the fix, 0 after. **Version + lifecycle:** feature/spec/contract version bumped 1.31.0 → 1.32.0 in three-way lockstep (Inv 36). **No source SKILL.md body changed, so no skill republish is required by this cycle.**
- **1.30.0 → 1.31.0 — housekeeping: correct dead `rabbit-feature-spec` reference in `rabbit-feature-scaffold` SKILL.md (issue #700, under #639):** The `rabbit-feature-scaffold` SKILL.md "What You Do NOT Do" section named the relocated `rabbit-feature-spec` skill as a do-not-invoke example. PROVE-IT-DEAD: `skills/rabbit-feature-spec/` does not exist (relocated to `rabbit-spec` as `rabbit-spec-create` / `rabbit-spec-update`). Reworded the example to the live successor `rabbit-spec-create`; bumped the SKILL.md frontmatter `version` 1.4.0 → 1.5.0 (deployed copy must be republished). Extended `test/test-spec-deadweight-removed.py` with `test_scaffold_skill_md_no_rabbit_feature_spec_reference` (RED→GREEN: failed on the dead reference before the fix, green after). This was the deferred item filed from the #685 pass.
- **1.29.0 → 1.30.0 — housekeeping round 2: measured dead-content removal from spec.md + contract.md (issue #685, under #639):** Retired Inv 26, 27, 28, 29, 30, 31 (the entire `rabbit-feature-spec SKILL.md` invariant section) and Inv 43 (the `prompts` section declaration). Removed the four `*(Withdrawn — folded into CHANGELOG.md.)*` tombstone placeholders for Inv 10, 11, 13, 42 (their prose already lives in this file; the per-number gap is preserved by the surviving numbering, not by a placeholder line — no test asserted the placeholders existed). This is a REMOVAL pass measured in lines deleted, not a reword. PROVE-IT-DEAD verification per #639:
  - **Inv 26–31 / `rabbit-feature-spec`:** the skill directory `skills/rabbit-feature-spec/` does not exist (`ls` empty) — it was relocated to the `rabbit-spec` feature (`rabbit-spec-create` / `rabbit-spec-update`, confirmed present under `.claude/features/rabbit-spec/skills/`). The mapped test `test/test-spec-skill.py` does not exist. DELETED the section header + Inv 26–31, the surface bullet, and the `provides.skills` entry in `contract.md` (test-contract-md.py only requires `surface.skills ⊆ provides.skills`, so dropping the extra entry is safe and tightens the contract).
  - **Inv 43 / `prompts`:** `feature.json.prompts` is `[]` (contradicting the invariant's "EXACTLY FIVE entries" mandate); the mapped test `test/test-prompts-declared.py` does not exist anywhere in the repo; the referenced `contract/templates/prompts/<id>.txt` templates for rabbit-feature ids do not exist. DELETED the section.
  - **Withdrawn tombstones (Inv 10, 11, 13, 42):** prose verified present in this CHANGELOG; `grep` confirmed no test references the placeholder lines. DELETED.
  - **Stale Tests-section mappings:** the section named three nonexistent files (`test-build-source.py`, `test-spec-skill.py`, `test-prompts-declared.py`) — all DELETED. The real `test-audit-owner.py` → Inv 50 mapping was added. The vocabulary/dead-content guards (`test-bb-vocab-retired.py`, `test-spec-deadweight-removed.py`) are NOT listed by name in the Tests section because their filenames contain a `retired`/`removed` substring that the contract Inv 49 strict-tier scanner flags; the section's lead-in already states `test/run.py` runs every `test-*.py`, so both guards run regardless.
  - **Contract.md lock-string corrections:** two `lock` strings named a nonexistent test file or wrong invariant number — `test-scope-script-resolve-scope.py (Inv 18)` → `test-scope-scripts.py (Inv 19)` (the real file; find-feature.py invocation is Inv 19, not Inv 18), and the validate-feature.py lock `(Inv 31, 33)` → `(Inv 35, 33)` (Inv 31 was the deleted rabbit-feature-spec invariant; the audit-side lock is Inv 35). These are factual corrections surfaced by the cleanup, now guarded by `test-spec-deadweight-removed.py`'s broadened live-surface ghost-test-file check (spec.md AND contract.md).
  - **Live-but-stale corrections (reword, not removal):** spec Inv 6 (`Skill("rabbit-feature-spec"...)` → `Skill("rabbit-spec-update"...)`, matching the live SKILL.md and the enforced `test_inv6_step_3_uses_spec_skill`), spec Inv 14 (the dead `rabbit-feature-spec` carve-out → `rabbit-spec-update`, matching the live SKILL.md Red Flags), and the Purpose paragraph ("four general-purpose helper skills" → "three", matching `feature.json.summary` and the actual `skills/` directory count). These three are factual corrections of references to a relocated skill, not net-zero churn.
  - **New E2E guard:** `test/test-spec-deadweight-removed.py` locks all four removals (no `rabbit-feature-spec` on live surfaces; Tests section names only existing files; no Withdrawn tombstones; `provides.skills` lists only shipped skills). RED→GREEN confirmed: 4 tests failed before implementation, all green after.
  - **Deferred (filed):** the `rabbit-feature-scaffold` SKILL.md "do not invoke" example still names the dead `rabbit-feature-spec`; correcting it requires a SKILL.md frontmatter bump + deployed-copy republish (out of TDD-subagent scope, would otherwise red the contract suite's `test-deployed-skills-match-source.py`). Filed as housekeeping enhancement #700.
  - **Version + lifecycle:** feature/spec/contract version bumped 1.29.0 → 1.30.0 in three-way lockstep (Inv 36); the stale `spec_no_change_reason` (from the prior no-change transition) was removed since this cycle carries real spec changes. **No source SKILL.md body changed, so no skill republish is required by this cycle.**

- **1.28.0 → 1.29.0 — legacy B/B ("bug-and-backlog") vocabulary retired from live surfaces (issue #664, part of #420):** No invariants retired, added, or renumbered. The legacy bug-and-backlog terminology was reworded to the current rabbit-issue vocabulary on the only two live surfaces that still carried it; the canonical `docs/spec.md` was already clean (verified zero hits). Changes:
  - **`docs/contract.md` (`dispatch-tdd-subagent.py` signature):** the documented signature listed stale, no-longer-accepted flags `[--linked-item <item-dir> --item-type bug|backlog]` and `[--linked-items <feature>:<type>:<id>[,...]]`. PROVE-IT-DEAD verification: the real script `.claude/features/tdd-subagent/scripts/dispatch-tdd-subagent.py` defines only `--scope`, `--spec`, `--impl-suggestion`, `--affected-invariants`, `--code-review-full-loop`, `--max-iterations` (no `--linked-item`/`--item-type`/`--linked-items` anywhere). The `bug|backlog` token was therefore B/B vocabulary, NOT a load-bearing enum. The signature was corrected to match the actual script (which also retires the B/B token).
  - **`skills/rabbit-feature-touch/SKILL.md` (`description`):** the metadata-only-write carve-out `(bug filing, backlog filing)` was reworded to `(filing a rabbit-managed issue, such as a bug or enhancement)`. Descriptive prose, not a literal.
  - **No load-bearing literals exempted.** A repo check confirmed no real branch name, regex token, enum value, or code-backed path on the live surfaces depends on a `backlog`/`B/B` token.
  - **New E2E guard:** `test/test-bb-vocab-retired.py` asserts no legacy B/B vocabulary (`B/B`, `bug-and-backlog`, `bug|backlog`, standalone `backlog`) remains on the live surfaces (`docs/spec.md`, `docs/contract.md`, `feature.json`, every `skills/*/SKILL.md`), with a documented `EXEMPT_SUBSTRINGS` escape hatch for any future load-bearing literal. RED→GREEN confirmed: 2 hits before implementation (contract.md signature, touch SKILL.md description), 0 after.
  - **Version + republish:** feature/spec/contract version bumped 1.28.0 → 1.29.0 in lockstep (Inv 36); `rabbit-feature-touch` SKILL.md frontmatter bumped 3.5.0 → 3.6.0, so it MUST be republished via the `publish_skill` manifest to re-sync its deployed `.claude/skills/` copy (the other three SKILL.md surfaces were unchanged). The Inv 49 strict tier is honoured: this CHANGELOG carries the issue pointers; the live surfaces stay history-free. **skill-creator validation note:** the `skill-creator:skill-creator` meta-skill is an interactive authoring workflow and cannot be invoked from inside a TDD subagent cycle, so the full skill-creator pass for this `description` reword is DEFERRED to a follow-up; the reword preserves triggering accuracy (it still names feature-directory writes vs. metadata-only issue filing).

- **1.27.0 → 1.28.0 — final `specs/` residual migrated to flat `docs/`; dead `specs/` fallback dropped (issue #591, closes the #399 residual):** No invariants retired or added; Inv 33, 46, 51, 54, 56 (and the secondary Inv 29, 30, 50 prose) reframed. This is the last #399 residual: the plugin-mode scaffolding and the spec/contract resolver were the only rabbit-feature surfaces still emitting / falling back to the legacy `specs/` layout. PROVE-IT-DEAD verification: `find .claude/features -path '*/specs/spec.md'` returned empty (all 11 features already on flat `docs/` from the #633/#556 work), and `grep` located the residual in `scaffold-feature.py` (`_scaffold_plugin_feature` created `specs/spec.md` + `specs/contract.md`) and `feature-touch.py` (`_resolve_doc` listed `specs/` as fallback rank 2). Changes:
  - **Plugin-mode scaffold (`scaffold-feature.py` `_scaffold_plugin_feature`)** now writes the flat `docs/spec.md` + `docs/contract.md` layout instead of `specs/`. The standalone scaffold was already on flat `docs/` (Phase 2a); only plugin mode lagged. Script version 2.2.0 → 2.3.0.
  - **Resolver (`feature-touch.py` `_resolve_doc`)** dropped the dead `specs/` fallback. Preference order is now flat `docs/` preferred → legacy `docs/spec/` fallback (the `docs/spec/` nested-docs fallback is retained as it is a distinct residual outside this issue's scope). A stray `specs/` file is no longer resolved. Script version 0.2.0 → 0.3.0.
  - **SKILL.md prose** — `rabbit-feature-scaffold` (description + Step 1/Step 3 scaffold-shape text) and `rabbit-feature-touch` (Step 3, Step 5, Red Flags spec-path notes) updated to drop `specs/` from the documented layout/fallback. `rabbit-feature-scaffold` frontmatter 1.3.0 → 1.4.0; `rabbit-feature-touch` 3.4.0 → 3.5.0. **Both SKILL.md changed and MUST be republished** via the `publish_skill` manifest to re-sync their deployed `.claude/skills/` copies.
  - **Tests** — `test-feature-new-plugin-mode.py` t1 now asserts plugin-mode scaffolds flat `docs/` and creates no `specs/`. `test-touch-docs-resolver.py` rewrote the two `specs/`-fallback cases to assert `specs/` is ignored (defaults to flat `docs/`) and renamed the both-exist cases to "flat docs wins over stray specs". `test-touch-skill-authoring-standard.py` `test_inv54_resolve_spec_path_prefers_specs_layout` → `_prefers_flat_docs` (asserts flat preferred, `specs/` ignored, `docs/spec/` fallback) and now asserts `specs/spec.md` is absent from the companion source. `test-touch-skill.py` `test_inv399_step5_delegates_spec_path_to_companion` → `test_inv56_step5_...`. RED→GREEN confirmed: 4 tests failed before implementation (plugin t1, resolver specs-ignored x2, authoring-standard companion-owns-mode-branching), all green after.
  - **Version + lifecycle:** feature/spec/contract version bumped 1.27.0 → 1.28.0 in lockstep; stale `spec_no_change_reason` (from the 1.26.0→1.27.0 no-change transition) removed since this cycle carries real spec changes. **skill-creator validation note:** the `skill-creator:skill-creator` meta-skill is an interactive authoring workflow and cannot be invoked from inside a TDD subagent cycle, so the full skill-creator pass for these SKILL.md prose changes is DEFERRED to a follow-up; the `description` fields (the triggering surface) had only the layout-name token updated, so triggering accuracy is preserved.

- **1.26.0 → 1.27.0 — declarative history-free doc surfaces + opt into the Inv 49 strict housekeeping tier (issue #555, Housekeeping Phase 2):** No invariants retired, added, or renumbered. rabbit-feature opted into the contract Inv 49 strict tier by declaring top-level `"housekeeping_clean": true` in its own `feature.json` (data-driven opt-in; no contract-owned file edited). All historical-burden framing was scrubbed from the three monitored doc surfaces — `docs/spec.md`, `docs/contract.md`, and the two SKILL.md surfaces that carried tags (`rabbit-feature-touch/SKILL.md`, `rabbit-feature-audit/SKILL.md`) — while substantive behaviour was preserved verbatim or rephrased to present-tense declarative. Concretely:
  - **Bare `#NNN` / "per issue" / "PR #NN" project tags removed** from spec.md (the `(issue #440)`, `(issue #399 …)`, `(issue #418)`, `per issue #468`, `per issue #399`, `(regression for #267)`, `(issue #416 Part C)` parentheticals across Inv 16, 50, 51, 52, 53, 54, 55, 56, 29, 33, 44 and the Tests section), from contract.md (the `scaffold-feature.py`, `audit-owner.py`, and `feature-touch.py` script-purpose strings), and from the two SKILL.md surfaces (`rabbit-feature-audit` `(issue #416 Part C)`, `rabbit-feature-touch` `per issue #399` and `(PR #93)`). The substantive behaviour each parenthetical annotated was kept; only the project-ticket pointer was dropped.
  - **Tombstone "Retired" wrappers reworded.** The four gap-preserver placeholder lines at the original invariant numbers 10, 11, 13, and 42 were changed from `*(Retired — see CHANGELOG.md.)*` to `*(Withdrawn — folded into CHANGELOG.md.)*`. The invariant numbers are NOT renumbered or removed — the gaps are preserved exactly as before; only the placeholder wording changed to drop the strict-tier tombstone word while still pointing readers to this CHANGELOG for the retired prose.
  - **Live `status: retired` references rephrased to cite the contract owner.** The genuine references to the contract-owned feature-status enum value (`audit-owner.py` / `validate_feature` short-circuit semantics in spec Inv 50, contract.md's `audit-owner.py` purpose, and the `rabbit-feature-audit` SKILL.md notes) were reworded to point at "contract Inv 36's status short-circuit" rather than restating the literal enum value. This keeps the behaviour accurate, respects Bounded Scope (the enum is defined and owned by `contract`, not redefined here), and avoids the strict-tier tombstone-word match — no contract ALLOWLIST entry was needed.
  - **Surfaces NOT touched:** `rabbit-feature-scope/SKILL.md` and `rabbit-feature-scaffold/SKILL.md` carried no tags and were left unchanged. No script, test, or schema body changed.
  - **Version + republish:** feature/spec/contract version bumped 1.26.0 → 1.27.0 in lockstep (Inv 36). The two SKILL.md bodies that changed had their frontmatter bumped — `rabbit-feature-touch` 3.3.0 → 3.4.0 and `rabbit-feature-audit` 1.1.0 → 1.2.0 — so BOTH must be republished via the `publish_skill` manifest to re-sync their deployed `.claude/skills/` copies. The strict tier (`#N`, "per issue/bug/pr", `superseded`/`retired`/`obsoleted`) now enforces on rabbit-feature; CHANGELOG.md is never scanned, so the relocated history here is exempt by construction. **skill-creator validation note:** the `skill-creator:skill-creator` meta-skill is an interactive authoring workflow and cannot be invoked from inside a TDD subagent cycle, so the full skill-creator pass for these SKILL.md prose changes is DEFERRED to a follow-up; the `description` fields (the triggering surface) are unchanged, so triggering accuracy is preserved.

- **1.25.0 → 1.26.0 — rabbit-feature's own doc artifacts relocated `specs/` → flat `docs/` (issue #399 Phase 2b):** No invariants retired or added. The feature's own spec/contract/CHANGELOG doc artifacts were moved into the flat `docs/` layout that the Phase 2a resolvers already prefer: `git mv specs/spec.md docs/spec.md`, `git mv specs/contract.md docs/contract.md`, and `git mv CHANGELOG.md docs/CHANGELOG.md`; the now-empty `specs/` directory was removed. `docs/bugs/` is PRESERVED — the doc artifacts land as siblings of `bugs/`, not nested under it. Spec/contract normative content is unchanged; only frontmatter `version` and intra-doc self-references were updated. Inv 36's prose now names the flat `docs/spec.md` / `docs/contract.md` location for rabbit-feature's own three-way version alignment (the invariant itself — feature/spec/contract version match — is unchanged). The spec.md header link to `feature.json` was corrected to the now-accurate `../feature.json` relative path. The skill resolvers and `scaffold-feature.py` are unchanged (already updated for the flat `docs/` layout in Phase 2a); plugin-mode scaffold (Inv 46) remains on `specs/` in this phase. Existing E2E regression test `test/test-specs-layout-migration.py` was rewritten to assert the flat `docs/` layout (docs/spec.md + docs/contract.md + docs/CHANGELOG.md present, docs/bugs/ retained, legacy `specs/` and root `CHANGELOG.md` gone, feature still validates). The own-file path constants in `test/test-version-sync.py`, `test/test-contract-md.py`, `test/test-audit-skill.py`, and `test/test-new-skill.py` were repointed from `specs/` to `docs/`. feature/spec/contract version bumped 1.25.0 → 1.26.0 in lockstep. No SKILL.md body changed, so source SKILL.md frontmatter versions are NOT bumped and no skill republish is required by this move. Deprecation criterion for the dual-read fallback is unchanged: when every rabbit feature has migrated to the flat `docs/` layout and the `specs/` / `docs/spec/` fallbacks are removed (issue #399 cleanup).

- **1.24.0 → 1.25.0 — spec/contract resolvers prefer the flat `docs/` layout (issue #399 Phase 2a):** Added Inv 56. The skill-local companion `skills/rabbit-feature-touch/scripts/feature-touch.py` resolvers now PREFER the flat `docs/` layout (`docs/spec.md`, `docs/contract.md` — the ratified #399 migration target), falling back to `specs/` and then the legacy `docs/spec/` layouts; when both a flat `docs/` and a `specs/` file exist, the flat `docs/` file wins, and resolution defaults to the flat `docs/` target when none exists yet. A new `resolve-contract-path <feature-name>` subcommand mirrors the spec preference order for the contract (Inv 54 amended). `scaffold-feature.py` standalone new-feature creation now writes to the flat `docs/` layout (`docs/spec.md`, `docs/contract.md`, preserving `docs/bugs/`) instead of `specs/` (Inv 33 amended); the scaffolded directory still passes `validate-feature.py` (which dual-reads flat `docs/` then `specs/` from Phase 1). Plugin-mode scaffolding (Inv 46) is unchanged in this phase. No existing files move — the repo is all on `specs/`, so the fallback always hits and the suites stay green during the coexistence window. New E2E regression test `test/test-touch-docs-resolver.py` locks the flat-docs preference order (docs/ wins over specs/, fallbacks, plugin-mode, commit under flat docs/) for both `resolve-spec-path` and `resolve-contract-path`. SKILL.md prose (Step 3 spec-commit, Step 5 spec-path resolution, Red Flags spec-write note) updated to name the flat `docs/` preference. Companion `feature-touch.py` version bumped 0.1.0 → 0.2.0; `scaffold-feature.py` 2.1.0 → 2.2.0; `rabbit-feature-touch` SKILL.md frontmatter 3.2.0 → 3.3.0; feature/spec/contract version bumped 1.24.0 → 1.25.0 in lockstep. Deprecation criterion for the dual-read fallback: when every rabbit feature has migrated to the flat `docs/` layout and the `specs/` / `docs/spec/` fallbacks are removed (issue #399 cleanup). **skill-creator validation note:** the `skill-creator:skill-creator` meta-skill is an interactive authoring workflow and cannot be invoked from inside a TDD subagent cycle, so the full skill-creator pass for this SKILL.md prose change is DEFERRED to a follow-up; the SKILL.md `description` field (the triggering surface) is unchanged, so triggering accuracy is preserved.

- **1.23.0 → 1.24.0 — `rabbit-feature-touch` SKILL.md §4 authoring-standard refactor (issue #440):** Added Inv 53, 54, 55 and reframed Inv 16 and Inv 51. Applies the SKILL.md Authoring Standard now codified in `policy/spec-rules.md` §4 (from #439 / PR #488) to the canonical `rabbit-feature-touch/SKILL.md`. The two inline bash blocks that combined `<feature-name>` placeholders with mode-aware `if/else` control flow — the Step 3 spec-commit and the Step 5 spec-path resolution — were §4 Script-Backed Orchestration violations (model-assembled orchestration). Their computed/mode-aware logic moved into a new skill-local companion script `skills/rabbit-feature-touch/scripts/feature-touch.py` exposing two subcommands: `commit-spec <feature-name> <summary>` (mode-aware `git add` / `git add -f`, empty-diff skip, `spec(<name>): update spec for <summary>` commit) and `resolve-spec-path <feature-name>` (specs/ preferred, docs/spec/ fallback, mode-aware). Step 3 and Step 5 now invoke the script; the prose obligations (the literal "Commit spec changes BEFORE Step 5" phrase, the commit-message pattern, the empty-diff-skip behaviour, and the specs/-preferred dual-read) are retained in the SKILL.md, and the dual-read mechanics are now OWNED BY and tested against the script source. Per §4 Verbatim Policy Embedding, the Red Flags section now cites the canonical bounded-scope policy (`policy/philosophy.md` §2, `policy/spec-rules.md` §2) rather than relying solely on a paraphrase. New regression test `test/test-touch-skill-authoring-standard.py` locks Inv 53/54/55 in both source and deployed SKILL.md (banned-pattern guard: no bash fence combining a runtime placeholder with control flow) and E2E-tests the companion script's spec-path resolution and commit/no-op behaviour in a temp git repo. The reframed Inv 16 / Inv 51 tests in `test/test-touch-skill.py` now assert the prose obligation plus the script delegation instead of the retired inline bash. SKILL.md frontmatter version bumped 3.1.0 → 3.2.0; feature/spec/contract version bumped 1.23.0 → 1.24.0 in lockstep; deployed copy re-synced via the `publish_skill` manifest. **skill-creator validation note:** the `skill-creator:skill-creator` meta-skill is an interactive authoring workflow and cannot be invoked from inside a TDD subagent cycle, so the full skill-creator pass for this change is DEFERRED to a follow-up; the deferral is recorded here, and because the SKILL.md `description` field (the triggering surface skill-creator validates) is unchanged by this refactor, triggering accuracy is preserved.

- **1.22.0 → 1.23.0 — dispatched agent type `tdd-subagent` → `rabbit-tdd-subagent` (issue #418, rabbit-feature half):** Added Inv 52. The `rabbit-feature-touch` SKILL.md Step 5 Agent tool call now passes `subagent_type: rabbit-tdd-subagent` (previously the call passed only `model` and `prompt`, with no explicit subagent type). This renames the dispatched AGENT type only; the prompt-assembler script path `.claude/features/tdd-subagent/scripts/dispatch-tdd-subagent.py` (feature-dir + script name) is unchanged and remains. New regression guards in `test/test-touch-skill.py` (`test_inv52_*`) lock the new agent type in both source and deployed SKILL.md and forbid the bare old `tdd-subagent` agent type outside the feature-dir/script-path reference. The companion `tdd-subagent`-feature half (renaming the `.claude/agents/tdd-subagent.md` manifest to `rabbit-tdd-subagent`) is tracked separately under #418.

- **1.21.1 → 1.22.0 — spec dir migration `docs/spec/` → `specs/` (issue #399 Phase 2):** The feature's own spec directory was moved with `git mv .claude/features/rabbit-feature/docs/spec .claude/features/rabbit-feature/specs`; `docs/bugs/` is retained (only `docs/spec` moved). The rabbit-feature-owned cross-feature tooling was updated in lockstep: `scaffold-feature.py` now creates new features (standalone and plugin mode) at the `specs/` layout (Inv 33, Inv 46); `rabbit-feature-touch` resolves a feature's spec at `specs/spec.md` preferring it, falling back to the legacy `docs/spec/spec.md` for not-yet-migrated features in both its Step 3 spec-commit diff check and its Step 5 `--spec` dispatch argument (Inv 16a); rabbit-feature's own spec/contract self-references were updated. Audit conformance accepts both layouts via the contract feature's `validate_feature` dual-read (Phase 1, merged upstream). Deprecation criterion for the dual-read fallback: when every rabbit feature has migrated off `docs/spec/`.

## Renumber and gap-preservation events

- **Phase 7c cleanup — rip B/B mode (this cycle):** Spec.md surviving invariants are NOT renumbered. This cycle retires two invariants (Inv 13 and Inv 42) — both asserted documentation in the `rabbit-feature-touch` SKILL.md for the B/B (bug / backlog) invocation mode of the seven-step sequence. The B/B mode itself is removed from the SKILL.md as part of this phase; with rabbit-file retired in Phase 7b there are no remaining callers passing `--linked-item` to `dispatch-tdd-subagent.py`, leaving the two invariants without any caller-side surface to bind against. Total retirements (2) is well within the renumber-vs-gaps threshold (≤ 5); gaps at positions 13 and 42 are preserved rather than re-flowing the surviving invariants, keeping all cross-references in tests and other features' specs stable. This continues the same gap-preservation pattern established by the Inv 10 / Inv 11 cascade.

- **TDD-SUBAGENT-BACKLOG-19 cascade:** Spec.md surviving invariants are NOT renumbered. The cascade retires two invariants (Inv 10 and Inv 11) — both asserted that the `rabbit-feature-touch` SKILL.md Step 4 documents passing `--human-approval-gate true|false` to `dispatch-tdd-subagent.py`. The upstream `tdd-subagent` feature removed the `--human-approval-gate` CLI flag (v5.0.0) when it dropped the SPEC-READ and HUMAN-APPROVAL steps from the dispatched prompt, so the two assertions no longer have a CLI surface to bind against. Total retirements (2) is well within the renumber-vs-gaps threshold (≤ 5); gaps at positions 10 and 11 are preserved rather than re-flowing the surviving invariants, keeping all cross-references in tests and other features' specs stable.

## Retired invariants

### Inv 10 — Step 4 bypass-active branch passes `--human-approval-gate false` (TDD-SUBAGENT-BACKLOG-19)
Originally asserted that the `rabbit-feature-touch` SKILL.md Step 4 documents passing `--human-approval-gate false` to the Step 5 `dispatch-tdd-subagent.py` invocation when the `.rabbit-human-approval-bypass` marker is present. Retired because the upstream `tdd-subagent` feature removed the `--human-approval-gate` argparse flag entirely in v5.0.0 (the subagent has no user-interaction tools, so the gate has no executor-side meaning). The dispatcher's bypass-active behaviour is now "proceed to Step 5 immediately" with no flag to pass. Replacement: a negative-assertion regression guard in `test/test-touch-skill.py` (`test_no_human_approval_gate_flag_in_source_skill` and `test_no_human_approval_gate_flag_in_deployed_skill`) ensures the flag string never reappears in either SKILL.md.

### Inv 11 — Step 4 bypass-absent branch documents `--human-approval-gate true` (TDD-SUBAGENT-BACKLOG-19)
Originally asserted that the `rabbit-feature-touch` SKILL.md Step 4 documents passing `--human-approval-gate true` (or omitting the flag, since `true` was the default) to the Step 5 `dispatch-tdd-subagent.py` invocation on the default marker-absent path. Retired with Inv 10 — the flag no longer exists in `dispatch-tdd-subagent.py` v4.0.0+, so the assertion has no CLI surface. The dispatcher's bypass-absent behaviour is now to wait for explicit user approval and then proceed to Step 5 (no flag to pass). Replacement: same negative-assertion regression guard as Inv 10.

### Inv 13 — B/B mode reads `item.json` via python3 (Phase 7c)
Originally asserted that the `rabbit-feature-touch` SKILL.md Step 1 B/B-mode block extracts the related feature from `<item-dir>/item.json` using `python3` (not `jq`, which is not a declared dependency) and references `item.json` (not the legacy `bug.json`). Retired because the entire B/B (bug / backlog) invocation mode was removed from the SKILL.md in the Phase 7c cleanup: with rabbit-file retired in Phase 7b there are no callers passing `--linked-item` to `dispatch-tdd-subagent.py`, so the Step 1 B/B-mode block (and the item.json read path it locks) no longer exists in the SKILL.md. The unified seven-step sequence is now single-mode (normal-mode-only) and resolves scope exclusively via `Skill("rabbit-feature-scope", ...)`.

### Inv 42 — B/B item materialization documented in SKILL.md (Phase 7c)
Originally asserted that the `rabbit-feature-touch` SKILL.md Step 1 contains a `#### B/B item materialization` subsection documenting (a) why materialization is needed (the canonical `bug-backlog-files` branch is never checked out), (b) the local mirror path layout under `.rabbit/rabbit/features/<feature>/<type>s/<id>/`, (c) the `git show origin/bug-backlog-files:...` fetch command, and (d) what gets passed to `--linked-item` (the local mirror directory). Inv 42 also required byte-identical presence of the subsection in both the source and deployed SKILL.md. Retired with Inv 13 — the B/B mode (and the materialization step it required) was removed entirely in the Phase 7c cleanup; with no caller passing `--linked-item`, neither the subsection nor the materialization workflow has a remaining surface to document. The byte-identity requirement between source and deployed SKILL.md is preserved by Inv 1 (build source) and the existing `test_inv41_source_and_deployed_byte_identical` test.
