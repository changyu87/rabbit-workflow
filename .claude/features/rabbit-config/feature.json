{
  "name": "rabbit-config",
  "version": "1.7.1",
  "owner": "rabbit-workflow team",
  "tdd_state": "test-green",
  "housekeeping_clean": true,
  "summary": "Configuration manager. Hosts the /rabbit-config skill (mutation surface) that dispatches subcommand invocations to contract.lib.mutation. At Stop and SessionStart, iterates every feature's CONFIGURATION declarations and emits alerts for active overrides.",
  "surface": {
    "skills": [
      "rabbit-config"
    ]
  },
  "deprecation_criterion": "when the rabbit CLI exposes native configuration mutation that subsumes this feature",
  "status": "active",
  "updated": "2026-06-04",
  "manifest": [
    {
      "api": "publish_skill",
      "args": {
        "source": "skills/rabbit-config/SKILL.md"
      }
    }
  ],
  "runtime": {
    "Stop": [
      {
        "api": "iterate_configurables_alerts",
        "args": {}
      }
    ],
    "SessionStart": [
      {
        "api": "iterate_configurables_banner",
        "args": {}
      }
    ]
  },
  "configuration": [],
  "prompts": [],
  "spec_no_change_reason": "Dead 'permissions lock/unlock' subcommand removal from SKILL.md only; spec.md has no references to this retired subcommand (its only 'permissions' mentions are the active bypass-permissions / permissions.defaultMode invariant)."
}
