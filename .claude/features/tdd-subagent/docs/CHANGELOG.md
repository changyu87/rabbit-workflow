---
feature: tdd-subagent
owner: rabbit-workflow team
deprecation_criterion: when the tdd-subagent spec's invariant numbering is folded into a structured schema-tracked log
---

# tdd-subagent — Retired Invariants Log

This file holds the tombstones for invariants that were previously declared in `docs/spec.md` and have since been withdrawn from the current design. Spec.md carries inline tombstones (one-line `*(Withdrawn; not part of the current design — see CHANGELOG.md.)*` markers); the full rationale lives here.

Each entry below carries the original invariant number (as it appeared in spec.md at the time of withdrawal), a one-line summary of what the invariant asserted and why it was withdrawn, and the backlog or change ID that drove it.

## Renumber and gap-preservation events

- **v5.24.0 — vendored-mode tdd-report path (#1058):** No invariants withdrawn or renumbered. `dispatch-tdd-subagent.py`'s `_tdd_report_path` mode check was changed from `== "plugin"` to membership in the existing module-level `_VENDORED_MODES = ("vendored", "plugin")` constant (added by #1050), mirroring the `_scope_marker_path` fix #1050 applied to the parallel resolver. This closes the follow-up that #1050's CHANGELOG entry explicitly flagged: `_tdd_report_path` carried the SAME single-value `== "plugin"` check, so after the `plugin`->`vendored` mode-value rename (#980) a vendored install (`.rabbit/.runtime/mode == 'vendored'`) fell through to the standalone report path `<repo_root>/.rabbit/tdd-report-<feature>.json` instead of the vendored `<rabbit_root>/tdd-report-<feature>.json`, mis-rooting the tdd-report. Inv 48's "Plugin mode" report-path bullet was rewritten to "Vendored mode" dual-accepting `vendored`/`plugin` via the shared `_VENDORED_MODES`. No second constant was introduced — the resolver reuses the one #1050 added. `dispatch-tdd-subagent.py` is a `publish_file` deployed script — its copy under `.claude/agents/tdd-subagent/scripts/` is synced in the SYNC-DEPLOYED step. Covered by `test/test-prompt-no-doubled-rabbit-paths.py` scenario C (e2e: `mode == 'vendored'` → STEP 7 `Path:` is the bare `tdd-report-<feature>.json` rooted at the rabbit-root, no standalone `.rabbit/` fall-through).

- **v5.23.0 — vendored-mode scope marker (#1050):** No invariants withdrawn or renumbered. `dispatch-tdd-subagent.py`'s `_scope_marker_path` mode check was changed from `== "plugin"` to membership in a new module-level `_VENDORED_MODES = ("vendored", "plugin")` constant, mirroring scope-guard's and `handoff-scaffold.py`'s `_VENDORED_MODES`. Root cause: after the `plugin`->`vendored` mode-value rename (#980), a vendored install (`.rabbit/.runtime/mode == 'vendored'`) fell through to the dashed standalone marker `.rabbit-scope-active-<feature>`, which scope-guard's vendored branch does NOT honor (it honors `.rabbit/.runtime/scope-active-<feature>`), so the dispatched subagent's in-scope writes stayed blocked — an adapter-ports cycle had to hand-create the vendored marker to unblock writes. Inv 12's "Plugin mode" bullet was rewritten to "Vendored mode" dual-accepting `vendored`/`plugin`. `dispatch-tdd-subagent.py` is a `publish_file` deployed script — its copy under `.claude/agents/tdd-subagent/scripts/` is synced in the SYNC-DEPLOYED step. NOTE: `_tdd_report_path` carries the same `== "plugin"` mode check and is a parallel latent bug under vendored mode (tdd-report would land at the standalone path); it is out of scope for #1050 and left for a follow-up. Covered by `test/test-prompt-lock-unlock-marker-path.py` scenario C (e2e: `mode == 'vendored'` → LOCK/UNLOCK emit `.runtime/scope-active-<feature>`, no standalone leak).

- **v5.22.0 — journal/resume research deliverable (#836; impl follow-up #838):** No invariants withdrawn or renumbered. Additive research-spike deliverable (Inv 27 research shape). Added a committed findings doc `docs/research/dispatch-journal-resume.md` answering whether the TDD dispatch can adopt a journal/resume mechanism to strengthen the loop WITHOUT moving the across-tick loop lifecycle onto the native Workflow tool. Verdict: **CAN** — a rabbit-native, on-disk per-tick dispatch journal is feasible and closes a real recovery gap, with the exact schema, write/read points, resume-skip algorithm, and way-in/way-out rollback specified in the doc. The analysis is grounded in the live recovery machinery (`fetch-queue.py` re-fetch, `merge-prs.py --record-pending` / `pending_post_merge`, `run-post-merge.py`, `clean-dispatch-leaks.py`, `prune-worktrees.py`, the `auto-evolve-state.schema.json` fields), and surfaces that `in_flight` is vestigial (declared/validated but never populated; never consulted by `fetch-queue.py`) so the journal would subsume it. The journal would be owned by rabbit-auto-evolve dispatch state; the implementation is filed as a separate enhancement (#838, feature:rabbit-auto-evolve). New presence/required-sections guard test `test/test-dispatch-journal-research-doc.py` (e2e; modeled on contract's #469 decision-doc guard) asserts the doc exists, carries lifecycle frontmatter declaring `feature: tdd-subagent`, records a clear CAN/CANNOT verdict, names the investigated state fields (`in_flight`, `pending_post_merge`), names a way IN and way OUT, and carries no bare `#NNN` tombstone. No owned scripts changed and the agent definition was NOT touched, so no republish is needed.

- **v5.21.0 — housekeep measured reduction wave (#817, final child of #794):** No invariants withdrawn or renumbered. `docs/spec.md` was cut from 798 to 785 lines by DELETING redundant restatement, restated rationale, and decorative parentheticals that prior waves only reworded — each proven redundant/dead at removal time, with no test asserting the removed text. Removed: (1) the ## Invariants preamble's "builds a slot dict and delegates prompt" sentence plus the Inv 23/24 bypass-note-placement restatement (current facts live in Inv 44/23/24); (2) Inv 37's "in practice `0o755` or `0o775`" decorative parenthetical (load-bearing rule is `mode & 0o100`); (3) Inv 50's "Distinct semantics → distinct verbs → distinct audit trail." restatement of the abort-vs-force sentence above it; (4) Inv 51's "Rationale:" clause whose accepted/rejected-state rule is fully stated above it; (5) Inv 49e's "Typical scoped prompts are 20-30KB … rabbit-config, rabbit-issue" decorative figures naming the retired rabbit-config feature (the size-win direction and the "no worse when omitted" guarantee are preserved); (6) Inv 47's "The same precedence ladder is shared with other find-feature.py callers" cross-feature informational aside that constrains no tdd-subagent behavior; (7) Inv 54's "This invariant documents the de facto behavior" meta-preamble (the HANDOFF-only rule stands on its own); (8) Inv 21's "even though the dispatcher no longer emits item-close instructions" past-state narration (the forward-compatibility retention fact per Inv 22 stands). All named load-bearing tokens (invariant ids, the 8-step cycle step names, code tokens, the `available: [1..29]` error string) are preserved; the substantive invariant/behaviour text is unchanged. New regression test `test/test-spec-housekeeping-817-dead-prose-removed.py` (e2e) asserts the deleted phrases stay absent and pins a 790-line ceiling so a future reword cannot re-inflate the body. No owned scripts changed and the agent definition was NOT touched, so no republish is needed.

- **v5.20.0 — migrate config refs to `/rabbit-tdd-autonomous` (phase 4 of #733):** No invariants withdrawn or renumbered. The bypass-marker preamble note (`_BYPASS_NOTE_TEXT` in `dispatch-tdd-subagent.py`, Inv 23) was migrated off the retired central `/rabbit-config human-approval true` re-enable hint and the old `human-approval` configurable label onto the per-feature command `/rabbit-tdd-autonomous false` (owned by rabbit-feature). Correct post-#336 polarity: `false` re-enables the Step-4 gate (revokes the bypass), so the note's hint reads "Re-enable the gate via `/rabbit-tdd-autonomous false`". The note now leads with the canonical marker `.rabbit-tdd-autonomous` and names the legacy `.rabbit-human-approval-bypass` as dual-read coexistence. `test/test-bypass-marker-note.py`'s `_EXPECTED_TEXT` was updated in lockstep. `docs/spec.md` overview (Step-4 tdd-autonomous approval gate) and Inv 23 wording were reframed; the stale "no configurable rename and no polarity flip" clause was corrected to state the #336 rename + polarity flip while noting the bypass-note emission is presence-only. `docs/contract.md` marker reads keep the dual-read design unchanged. The legacy marker NAME `.rabbit-human-approval-bypass` survives only as dual-read coexistence. `dispatch-tdd-subagent.py` is a deployed script — its copy under `.claude/agents/tdd-subagent/scripts/` drifts until the dispatcher republishes (`deployed-copies-match` RED for `dispatch-tdd-subagent.py` until republish). The contract `_resolve_marker_value` polarity bug is out of scope (filed as #775).

- **v5.19.0 — housekeeping round 2 (#689 under #677; measured line-removal pass):** No invariants withdrawn or renumbered. `docs/spec.md` was cut from 906 to 795 lines (−111, −12.2%) by DELETING dead/historical/redundant prose that round-1 only reworded. Each deletion was proven dead/redundant by a #639 deterministic check at removal time: (1) the "Note on the prompt-contract migration" preamble — historical migration narration, current fact stated in Inv 44; (2) Inv 12's "Before this invariant was made mode-aware…" inert-plugin-marker bug narration; (3) Inv 44's "`_policy_block` helper … MUST be removed" one-time directive — `grep` of dispatch-tdd-subagent.py returns zero matches for `_policy_block`/`policy-block.py` (proven done) — plus the f-string byte-equivalence regression-net narration; (4) Inv 47's "Mirroring constraint…" cross-feature note with a rotting `lines ~60-70` pin, and the verbose multi-scenario test restatements in Inv 46/47/48/49/55/56 collapsed to one-line enforcement pointers; (5) Inv 57's deadlock/"observed effect" bug narration; (6) Inv 58/60's "**Why.** Previously…" worktree-leak bug narration; (7) Inv 29's "Pre-v4.0.0 this entry lived on tdd-state-machine" provenance; (8) Inv 31's "added in v5.1.0" version pin; (9) Inv 45/55's stale `template_version` bump directives (template is past those versions). `docs/contract.md`: the dead `invokes.scripts` entry `policy-block.py` (zero callers in the dispatcher; `build-prompt.py` is the actual invocation) was corrected. The substantive invariant/behaviour text is preserved present-tense. New regression test `test/test-spec-housekeeping-689-dead-prose-removed.py` (e2e) asserts the deleted phrases stay absent and an 820-line ceiling so a future reword cannot re-inflate the body. No owned scripts changed and the agent definition (`agents/rabbit-tdd-subagent.md`) was NOT touched, so no republish is needed.

- **v5.18.0 — housekeeping Phase 2 (declarative history-free doc surfaces; opt into Inv 49 strict tier):** No invariants withdrawn or renumbered. `docs/spec.md` and `docs/contract.md` were scrubbed of historical-burden tags (bare `#NNN` issue/PR refs, "per issue/PR" prose, and the `superseded`/`retired`/`obsoleted` tombstone vocabulary) so the doc surfaces describe only the CURRENT design; the substantive invariant/behaviour text is preserved and rewritten present-tense. The feature opts into the strict tier of `test-spec-bodies-no-historical-tags.py` by declaring top-level `"housekeeping_clean": true` in `feature.json` (data-driven opt-in via `derive_cleaned_features()`; no contract-owned file edited). Inline withdrawn-invariant tombstones changed from `*(Retired — see CHANGELOG.md.)*` to `*(Withdrawn; not part of the current design — see CHANGELOG.md.)*`. Historical justification prose moved here: the SYNC-DEPLOYED drift class (Inv 46) was the source of four fix-up commits between two consecutive PRs; the downstream-test-discovery gap (Inv 56) was motivated by a PR that retired the Skill-path injection and silently broke 15 rabbit-cage tests whose `install.py` and fixtures referenced the removed paths. The `blocked-by-#NNN` reason-code example (Inv 50/55) was generalized to `blocked-by-<issue>`. No owned scripts changed and no deployed surface changed, so no republish is needed.

- **issue #561 (v5.17.0 — `tdd-step.py` resolves `rabbit_print` from the repo root so it works from the deployed location):** No invariants retired or renumbered. Added Inv 62 (`rabbit_print` import resolves from the repo root, not a fixed `parents[N]` offset). Root cause: the import-time path computed `_Path(__file__).parents[2] / "contract" / "scripts"`, an offset valid ONLY at the SOURCE copy depth (`.claude/features/tdd-subagent/scripts/`, whose `parents[2]` is `.claude/features/`). At the DEPLOYED copy depth (`.claude/agents/tdd-subagent/scripts/`, whose `parents[2]` is `.claude/`) it resolved to the nonexistent `.claude/contract/scripts`, so EVERY invocation — including `--help` — raised `ModuleNotFoundError: No module named 'rabbit_print'`, making the scripted TDD-step driver unusable from the deployed location. The #583 cwd-based `_repo_root` fix did NOT cover the import path (the import runs at module load, before `_repo_root` is defined). Fix: a self-contained `_contract_scripts_dir()` import-time resolver anchors on the repo root with precedence (a) `RABBIT_ROOT` (plugin mode, Inv 47), (b) cwd-based `git rev-parse --show-toplevel` (consistent with Inv 60), (c) a robust upward walk from the script's own location; each candidate is accepted only when `rabbit_print.py` actually exists under `<root>/.claude/features/contract/scripts`. `tdd-step.py` is a `publish_file` deployed script — its deployed copy under `.claude/agents/tdd-subagent/scripts/` drifts until republished (`deployed-copies-match` RED for `tdd-step.py` until the dispatcher republishes). Covered by `test/test-rabbit-print-import-from-deployed.py` (e2e: real git repo staging both the source and deployed layouts + the contract `rabbit_print.py`; `tdd-step.py --help` from each copy loads the module and exits 0).
- **issue #601 (v5.16.0 — test fixture resolves rabbit-cage spec at flat docs/ layout):** No invariants retired or renumbered. Test-only fix. `test/test-affected-invariants-flag.py`'s local `_resolve_fixture_spec` helper hardcoded a stale dual-read order (`specs/spec.md` preferred, legacy `docs/spec/spec.md` fallback) — neither path exists after the #399 flat-docs migration, so the test `FileNotFoundError`ed on a clean dev checkout. The helper now delegates to the shared `_helpers._resolve_spec_md` resolver (flat `docs/spec.md` preferred, `specs/spec.md` fallback), so the rabbit-cage fixture resolves to its current flat location and survives future relayouts. Test intent is unchanged — only the path resolution was wrong; the four scenarios (full embed, scoped subset, unknown-number fatal, size win) are untouched. No owned scripts changed and tests are not deployed, so no republish is needed.
- **issue #528 (v5.15.0 — oversized prompt slots bypass the argv 128 KB cap):** No invariants retired or renumbered. Added Inv 61 (oversized prompt slots bypass the argv `MAX_ARG_STRLEN` cap). Root cause: `dispatch-tdd-subagent.py` passed the entire `spec_content` (and `impl_suggestion_block`) as a single `--slot name=value` argv string to `build-prompt.py`; Linux caps any one argv string at `MAX_ARG_STRLEN` (128 KB, independent of `ARG_MAX`), so a large feature spec (e.g. the ~148 KB rabbit-auto-evolve spec) made the subprocess `exec()` die with `OSError: [Errno 7] Argument list too long`. Fix: the dispatcher now budgets each `name=value` against 128 KB; any slot over budget is passed as a tiny `os.urandom`-derived sentinel token (wrapped in `0x1e` record-separator chars — NUL-free so it is legal in argv, and never present in spec/template prose) and the real value is substituted back into the assembled prompt via a single `str.replace` per oversized slot AFTER `build-prompt.py` returns. `build-prompt.py` and the prompt template are owned by the `contract` feature (Out of Scope here), so the round-trip is internal to the dispatcher — its CLI is unchanged and Inv 44's "one `--slot` per declared slot" shape is preserved. In-range inputs take the exact same argv path and yield a byte-identical prompt (verified by diff). `dispatch-tdd-subagent.py` is a `publish_file` deployed script — its deployed copy under `.claude/agents/tdd-subagent/scripts/` drifts until republished. Covered by `test/test-large-slot-no-argv-limit.py` (e2e: >128 KB `spec_content` → exit 0, no argv error, oversized body present verbatim).
- **issue #583 (v5.14.0 — cwd-based repo-root resolution):** No invariants retired or renumbered. Added Inv 60 (repo-root resolution targets the operating worktree via the process cwd, not the script's own location). Both owned scripts' `_repo_root` resolver changed from `git -C <script_dir> rev-parse --show-toplevel` to `git rev-parse --show-toplevel` (run in the process cwd); `RABBIT_ROOT` (plugin mode, Inv 47) still takes precedence verbatim. Root cause: under worktree-isolated phase-5 dispatch the subagent invokes the MAIN deployed copy of `tdd-step.py`, so script-dir resolution pointed `_repo_root` (and the `.rabbit-scope-active-<feature>` marker + relativized `feature.json` bookkeeping anchored on it) at the dispatcher's MAIN tree, leaking writes there and tripping `safety-check.py` Inv 5. `dispatch-tdd-subagent.py`'s `_repo_root(script_dir)` was aligned to the same cwd-based resolution (the `script_dir` parameter is retained for call compatibility but no longer drives resolution). Both `tdd-step.py` and `dispatch-tdd-subagent.py` are `publish_file` deployed scripts — their deployed copies under `.claude/agents/tdd-subagent/scripts/` drift until republished. Covered by `test/test-repo-root-cwd-resolution.py` (e2e: real git repo + linked worktree).
- **issue #399 Phase 2b (v5.13.0 — specs/ -> flat docs/ file move):** No invariants retired or renumbered. The feature's own doc artifacts moved from `specs/` and the feature root to the flat `docs/` layout (`git mv` preserves history): `specs/spec.md` -> `docs/spec.md`, `specs/contract.md` -> `docs/contract.md`, and the feature-root `CHANGELOG.md` -> `docs/CHANGELOG.md`; the now-empty `specs/` directory was removed. Spec/contract bodies are unchanged save for the version bump (5.12.0 -> 5.13.0) and the contract `reads.files` self-reference, which now names `<feature-dir>/docs/spec.md` as preferred (with `specs/` and legacy `docs/spec/` as coexistence-window fallbacks). No owned scripts changed — `tdd-step.py` / `dispatch-tdd-subagent.py` already dual-read the flat `docs/` layout (Phase 2a), so no republish is needed; the move only relocates the doc artifacts the resolver already prefers. `test/test-specs-migration.py` and `test/_helpers.py` were updated to assert/resolve the flat `docs/` layout; `test/test-agent-rename-rabbit.py` now reads `docs/contract.md`. Covered by `test/test-specs-migration.py` (e2e). Phase 3 drops the `specs/` fallback once every feature has migrated.
- **issue #399 Phase 2a (v5.12.0 — flat docs/ spec-path resolver):** No invariants retired or renumbered. Added Inv 59 (spec/contract path resolution prefers the flat `docs/` layout). `tdd-step.py` gained a `resolve_spec_path(feature_dir, name)` helper (prefer `<feature-dir>/docs/<name>`, fall back to `<feature-dir>/specs/<name>`) mirroring `contract.lib.checks.resolve_spec_path`; the now-redundant directory-based `_resolve_spec_dir` helper was removed. The Inv 38 spec-update gate diffs the flat `docs/spec.md` and `docs/contract.md` candidates in addition to `specs/` and legacy `docs/spec/`; the Inv 41 numbered-list check runs against the resolved `spec.md` / `contract.md` files (flat `docs/` preferred, `specs/` then legacy `docs/spec/` as fallbacks) instead of sweeping the whole `docs/` tree. `dispatch-tdd-subagent.py` (v4.4.0) scoped-view grep hint now falls back to the flat `docs/spec.md` layout (when present under repo root) before the `specs/spec.md` layout. No files moved this phase; the repo is still on `specs/`, so the fallback always hits. Covered by `test/test-docs-resolver.py`. Phase 3 drops the `specs/` fallback once every feature has migrated.
- **issue #336 Phase 1 (v5.9.1 — dispatch dual-read bypass marker):** No invariants retired or renumbered. Inv 23 (bypass-marker note emission) was rewritten in place: `dispatch-tdd-subagent.py` now treats the human-approval bypass as active when EITHER `.rabbit-human-approval-bypass` OR `.rabbit-tdd-autonomous` exists at the repo root (dual-read), and the `_BYPASS_NOTE_TEXT` constant names both marker forms. This is the coexistence-window read step only — no configurable rename and no polarity flip. The deployed copy at `.claude/agents/tdd-subagent/scripts/dispatch-tdd-subagent.py` was re-synced via the `publish_file` manifest entry. `test/test-bypass-marker-note.py` now exercises three cases: neither marker (no note), legacy `.rabbit-human-approval-bypass` present (note), and `.rabbit-tdd-autonomous` present (note). Issue #336 stays open until later phases land.
- **issue #418 (v5.9.0 — agent manifest item rename tdd-subagent -> rabbit-tdd-subagent):** No invariants retired or renumbered. Added Inv 57 (agent manifest item name). The agent definition file was renamed `agents/tdd-subagent.md` -> `agents/rabbit-tdd-subagent.md` (git mv), its `name:` frontmatter changed to `rabbit-tdd-subagent`, and the `publish_agent` manifest entry + contract `provides.agents` path updated; the manifest-driven publish loop now deploys `.claude/agents/rabbit-tdd-subagent.md` (legacy `.claude/agents/tdd-subagent.md` no longer produced). Scoped to the AGENT manifest item only: the feature directory, the `dispatch-tdd-subagent.py` / `tdd-step.py` script basenames, the `prompts[].id` / template `tdd-subagent.txt`, and the agent-adjacent `.claude/agents/tdd-subagent/scripts/` deploy dir all keep their `tdd-subagent` names. Companion PRs cover the out-of-scope `install.py` AGENTS list and other repo-level references; issue #418 stays open until they land.
- **issue #399 Phase 2 (v5.8.0 — docs/spec/ -> specs/ migration):** No invariants retired or renumbered. The feature's own spec/contract moved from `docs/spec/` to `specs/` (git mv; empty `docs/` removed). Inv 38 (spec-update gate) and Inv 41 (numbered-list check) were rewritten in place to resolve the spec dir dual-read (`specs/` preferred, legacy `docs/spec/` fallback during the #399 coexistence window); Inv 49's scoped-view grep NOTE now points at the resolved `--spec` path. `dispatch-tdd-subagent.py` (v4.2.0) and `tdd-step.py` were made specs-aware so dispatches still work for not-yet-migrated features. Phase 3 drops the docs/spec/ fallback once every feature has migrated.
- **Phase 7c (drop --linked-item / --linked-items / --item-type CLI surface):** Spec.md surviving invariants are NOT renumbered. This cycle retires four invariants (Inv 6, 19, 20, 30) and simplifies one (Inv 21 — `closed_items` is now always an empty list but the field is retained on the HANDOFF schema for forward compatibility per Inv 22). Numbering gaps at positions 6, 19, 20, 30 are preserved rather than re-flowing the surviving invariants. Total retirement count (4) is within the housekeeping protocol's renumber-vs-gaps threshold (≤ 5); the cross-reference cost of a cascade rewrite outweighs the gap cost. Inv 3 (exit codes), Inv 4 (CLI flag set), and Inv 45 (prompts section slots) were rewritten in place — they keep their numbers because the surrounding invariants still reference those numbers.
- **TDD-SUBAGENT-BACKLOG-19 (drop redundant SPEC-READ + HUMAN-APPROVAL steps):** Spec.md surviving invariants are NOT renumbered. This backlog retires four invariants (Inv 5, 13, 25, 26) and preserves the resulting numbering gaps at those positions rather than re-flowing the surviving invariants. Total retirement count (4) is within the housekeeping protocol's renumber-vs-gaps threshold (≤ 5); the cross-reference cost of a cascade rewrite outweighs the gap cost. Inv 4 (CLI flag set) and Inv 8 (step-banner list) were rewritten in place — they keep their numbers because the surrounding invariants still reference those numbers.

## Retired invariants

### Inv 6 — `--linked-items` triple validation (Phase 7c)
Originally asserted that the dispatcher validated `--linked-items` entries (comma-separated `<feature>:<type>:<id>` triples) for exactly two colons, non-empty fields, and a type in {bug, backlog}, emitting a stderr diagnostic and exit 2 on any malformed entry BEFORE the prompt was written to stdout. Retired alongside the `--linked-items` flag itself: secondary linked-item closure was wired through the now-removed `rabbit-file` feature's `item-status.py`, and that feature was retired in earlier phases. With no downstream consumer of the parsed triples, the validator and its surrounding parse loop are dead code. The flag was removed from the argparse surface in `dispatch-tdd-subagent.py`; argparse now rejects `--linked-items` as an unrecognized argument. Equivalent assertion: spec Inv 4 (flag set excludes `--linked-items`), `test/test-cli-invocation.py` (asserts argparse rejects `--linked-items` as an unrecognized argument).

### Inv 19 — primary `--linked-item` closure (Phase 7c)
Originally asserted that when `--linked-item` + `--item-type` were supplied, the assembled prompt's STEP 7 UNLOCK section contained an `item-status.py set --feature <f> --type <t> --id <i> --status close --reason 'TDD cycle complete' --fix-commits $IMPL_SHA` block to close the primary item after the impl commit. Retired with the `rabbit-file` feature: there is no longer an `item-status.py` script to invoke, and item-state management was relocated outside the TDD cycle. The corresponding close-call rendering block was deleted from `dispatch-tdd-subagent.py`; the flag was removed from the argparse surface.

### Inv 20 — secondary `--linked-items` closure (Phase 7c)
Originally asserted that every secondary entry in `--linked-items` produced an additional `item-status.py set ... --reason 'TDD cycle complete (secondary item resolved by same commit)' ...` block in STEP 7 UNLOCK. Retired with Inv 19 for the same reason: the downstream `item-status.py` script no longer exists. The flag and its rendering loop were removed.

### Inv 21 — HANDOFF `closed_items` list (Phase 7c — simplified, not retired)
Originally asserted that HANDOFF's `closed_items` field listed every item closed by the cycle (primary + secondaries). With the `--linked-item` / `--linked-items` surface gone (Inv 19, 20), there are no items to close, so `closed_items` is now always an empty list `[]`. Inv 21 is **simplified rather than retired**: the field is retained on the HANDOFF schema for forward compatibility per Inv 22 (HANDOFF schema versioning), and a future cycle reintroducing item-close machinery would simply repopulate the list without a schema bump. The simplified invariant text lives inline in spec.md at Inv 21.

### Inv 30 — `--linked-item` path-layout validation (Phase 7c)
Originally asserted that `dispatch-tdd-subagent.py` resolved the `--linked-item` path and required the canonical `.../rabbit/features/<feature>/<bugs|backlogs>/<id>/` layout, exiting 2 with a stderr diagnostic on a mismatch and wiring the validated feature name (segments[-3]) through to the close-call block. Retired with Inv 19: with no downstream close-call block to consume the validated feature name, the layout check is dead code. The `_validate_linked_item` helper was deleted from `dispatch-tdd-subagent.py` and the flag itself was removed.

### Inv 5 — boolean flag vocabulary (TDD-SUBAGENT-BACKLOG-19)
Originally asserted that boolean values for `--human-approval-gate` are exactly `true` or `false` (no `enabled`/`disabled`, `yes`/`no`, etc.). Retired alongside the `--human-approval-gate` flag itself: the flag was removed from the argparse surface in `dispatch-tdd-subagent.py`, so there is no boolean vocabulary left to constrain. The wider boolean-vocabulary convention (true/false only) is still observed by other rabbit features that accept boolean flags, but it is not specific to tdd-subagent any more. Equivalent assertion: spec Inv 4 (flag set excludes `--human-approval-gate`), `test/test-cli-invocation.py` (asserts argparse rejects `--human-approval-gate true` and `--human-approval-gate false` as unrecognized arguments).

### Inv 13 — SPEC-READ diff target (TDD-SUBAGENT-BACKLOG-19)
Originally asserted that the assembled prompt's STEP 1 SPEC-READ section runs `git diff HEAD~1 -- <feature_dir>/docs/spec/`. The STEP 1 SPEC-READ section was deleted from the prompt template altogether: spec context is already interpolated verbatim into the prompt via `{spec_content}`, so an in-subagent diff against HEAD~1 was redundant (the subagent has the post-edit spec in front of it; the dispatcher's spec-authoring step produced the committed baseline). With the SPEC-READ section gone, there is no diff command for any test to assert.

### Inv 25 — `--human-approval-gate true` branch (TDD-SUBAGENT-BACKLOG-19)
Originally asserted that with `--human-approval-gate true` (default), the assembled prompt contains a STEP 2 HUMAN-APPROVAL section instructing the subagent to invoke `Skill("superpowers:writing-plans")`, present the implementation summary to the user, and wait for explicit approval before STEP 3 LOCK. Retired because subagents have no user-interaction tools — the "wait for explicit approval" instruction was dead code (subagents cannot prompt the user, and the dispatcher's Step 4 HUMAN-APPROVAL gate is the real approval surface). The STEP 2 HUMAN-APPROVAL section was deleted from the prompt template. The `--human-approval-gate` flag was removed from the argparse surface alongside.

### Inv 26 — `--human-approval-gate false` branch (TDD-SUBAGENT-BACKLOG-19)
Originally asserted that with `--human-approval-gate false`, the assembled prompt's STEP 2 HUMAN-APPROVAL section is a one-line stub stating the step is skipped. Retired with Inv 25 — the STEP 2 HUMAN-APPROVAL section no longer exists in either branch (there is no longer a flag to branch on), so there is no stub form to assert.
